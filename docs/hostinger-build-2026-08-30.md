# تقرير حزمة Hostinger — 30 أغسطس 2026

## الملخص التنفيذي

تم استكمال مسار تحسين الصور وإعادة بناء حزمة Hostinger من آخر مصدر في المشروع. الحزمة النهائية جاهزة للرفع، وتحتوي على صور WebP الجديدة الخاصة بصور حالات الحاويات فقط، مع إبقاء ملفات PNG الأصلية كـ fallback صريح داخل `<picture>` حتى لا تتأثر المتصفحات القديمة أو الروابط السابقة.

تم التحقق من الأرشيف بعد استخراجه مؤقتًا، وليس من مجلد البناء فقط. كما اجتازت النسخة النهائية بوابات النوع والتشغيل وSEO وPHP وسلامة ZIP.

## الملفات المسلّمة

النسختان التاليتان متطابقتان byte-for-byte:

- `taqi-group-hostinger.zip` — الاسم التوافقي المعتمد للرفع.
- `cleanflow-services-hostinger.zip` — الاسم الرسمي الجديد.

بيانات النسخة:

- **الحالة:** جاهزة للرفع إلى Hostinger.
- **النطاق المستخدم في البناء:** `https://taqigroup.com`.
- **وقت البناء:** `2026-08-29T21:37:46.087Z` UTC، الموافق 23:37:46 بتوقيت القاهرة.
- **الحجم:** `38,350,527` بايت، حوالي `37.45 MiB`.
- **SHA-256:** `b6002bfb8a0dcfa03d9d200a6c802ac2ee8d61539422bc331d9984f963bb76e7`.
- **عدد عناصر الأرشيف:** `922`.
- **طريقة التشغيل:** PHP 8.x + PDO SQLite فقط؛ لا تعتمد الحزمة على Node.js أو Replit بعد الرفع.

## التعديلات المنفذة

### 1. صور حالات الحاويات

تم ربط الصور الأربعة بصيغة WebP داخل واجهة نظام الحاويات، مع الاحتفاظ بملف PNG المطابق كبديل حقيقي:

| الحالة | WebP | PNG fallback | الأبعاد |
|---|---:|---:|---:|
| أزرق / حالة أخرى | 95,640 بايت | 702,834 بايت | 1200×600 |
| أخضر / متاحة | 96,274 بايت | 698,405 بايت | 1200×600 |
| أحمر / مؤجرة | 90,532 بايت | 692,548 بايت | 1200×596 |
| أصفر / صيانة | 92,582 بايت | 691,595 بايت | 1200×600 |

النتيجة الإجمالية:

- حجم PNG الأصلي للصور الأربع: `2,785,382` بايت.
- حجم WebP للصور الأربع: `375,028` بايت.
- الوفر: `2,410,354` بايت، أي حوالي `86.54%`.
- لم يتم اعتبار أي من هذه الصور صورة للصفحة الرئيسية؛ استخدامها محصور في صور حالة الحاوية.

التنفيذ يحافظ على السلوك التالي:

- المتصفحات التي تدعم WebP تطلب ملف `.webp` الأصغر.
- المتصفحات التي لا تدعم WebP تستخدم ملف `.png` الموجود فعليًا.
- تم وضع `width` و`height` وفق الأبعاد الأصلية، بما في ذلك اختلاف الصورة الحمراء، لتقليل layout shift.
- تمت إضافة `loading="lazy"` و`decoding="async"` لهذه الصور.

### 2. تحسينات إضافية للأداء والثبات البصري

- إضافة أبعاد وتحميل كسول للصور الإعلانية وصورة قسم «لماذا نحن».
- إضافة أبعاد ثابتة لشعاري شريط التنقل والتذييل.
- استبدال حركة `framer-motion` في انتقالات Hero والعناصر العائمة بانتقالات CSS أخف، مع الإبقاء على الحركة والتفاعل.
- إضافة حركة دخول CSS لأزرار الاتصال وواتساب.
- جعل تحميل خط Cairo غير حاجب للرسم الأول، مع fallback للنص ونسخة `<noscript>`.
- إضافة ضغط نصوص Apache/Brotli عند توفر الوحدات، وسياسات cache مناسبة للأصول المضمّنة.

### 3. تحسينات بناء Hostinger

- تنظيف ناتج `build_php` في كل بناء قبل النسخ حتى لا تبقى صفحات أو أصول من إصدار سابق.
- إبقاء قاعدة SQLite وطبقة PHP وملفات الأصول داخل الأرشيف النهائي.
- الحفاظ على ملفات التوافق القديمة المطلوبة للروابط السابقة دون إدخالها في Sitemap كصفحات قابلة للفهرسة.
- إصلاح `scripts/test-php-api.mjs` ليستعمل مسار المشروع الحالي بدل مسار Windows القديم `e:/Hawiat`.
- إنشاء النسخة التوافقية `taqi-group-hostinger.zip` كنسخة مطابقة للأرشيف الرسمي، وليس كأرشيف مستقل قديم.

## محتوى قاعدة البيانات المضمّنة

تم تضمين قاعدة `data/sabaik.db` بعد فحص سلامتها:

- `integrity_check = ok`.
- `104` سجلًا في نظام سجلات الحاويات.
- `161` سجل تدقيق لنظام الحاويات.
- `69` مقالة.
- `19` صفحة SEO.
- `5` شرائح Hero.
- `2209` مشاهدة صفحات.
- `1` طلب خدمة.
- `6` حسابات إدارة.

## نتائج التحقق النهائية

### بوابات الكود

- `pnpm run typecheck` — **PASS**.
- فحص Prettier للملفات المعدلة — **PASS**.
- `pnpm --filter @workspace/scripts run validate:operational` — **PASS**:
  - public tracking projection
  - financial idempotency
  - container route permission
  - driver transition protection
- `git diff --check` — **PASS**.

### بوابة SEO والأرشيف المستخرج

- `SITE_URL=https://taqigroup.com pnpm --filter @workspace/scripts run seo-quality-gate` — **PASS**.
- `152` رابطًا فريدًا في Sitemap.
- `152/152` صفحة قابلة للفهرسة مطابقة للـ Sitemap.
- `152/152` صفحة لديها canonical صحيح على HTTPS.
- `152/152` صفحة لديها Meta Description ضمن نطاق `120–160` حرفًا.
- `152/152` صفحة لديها JSON-LD صالح.
- `152/152` صفحة لديها روابط داخلية قابلة للفحص.
- `207` صورة في Sitemap، وجميعها موجودة داخل الأرشيف.
- `170` صورة مستخدمة في HTML، وجميعها موجودة داخل الأرشيف.
- لا توجد روابط localhost أو Replit داخل Sitemap.
- تم استخراج الأرشيف وفحص `index.html` و`robots.txt` و`api/index.php` وواجهة المنصة والأصول المشار إليها.

### PHP وZIP

- `php -l build_php/api/index.php` — **PASS**.
- `php -l build_php/api/container-system.php` — **PASS**.
- `unzip -t taqi-group-hostinger.zip` — **PASS**.
- `cleanflow-services-hostinger.zip` و`taqi-group-hostinger.zip` — **متطابقان**.
- تم التأكد من وجود ملفات WebP وPNG الأربعة داخل الأرشيف المستخرج، ومن أن WebP ملفات WebP فعلية وليست PNG باسم مختلف.
- لم يحتوِ الأرشيف على `.env` أو `.git` أو `node_modules` أو نسخة `build_php` داخلية.

### الفحص التجريبي لمسار PHP

تم تشغيل `node scripts/test-php-api.mjs` بعد إصلاح مسار Windows. يعيد الاختبار `Unauthorized` لأن الطلب التجريبي يستدعي مسارًا إداريًا دون جلسة دخول، وهذا سلوك الحماية المتوقع، بينما ينتهي أمر الفحص نفسه دون crash. لا ينبغي اعتبار هذا الطلب تسجيل دخول ناجحًا؛ الاختبار مخصص للتأكد من تشغيل ملف PHP والوصول إلى طبقة الحماية.

### المعاينة المحلية

- واجهة الخدمات أعيد تشغيلها بعد آخر بناء.
- Workflow الواجهة يعمل بنجاح.
- معاينة الصفحة الرئيسية ظهرت دون أخطاء JavaScript تشغيلية.
- سجلات المتصفح احتوت فقط على رسائل Vite/React DevTools المعلوماتية.
- API وواجهة المنصة ما زالا يعملان في workflows الخاصة بهما.

## طريقة الرفع إلى Hostinger

1. خذ نسخة احتياطية من مجلدي `data/` و`uploads/` الحاليين.
2. استخرج محتويات `taqi-group-hostinger.zip` مباشرة داخل `public_html`.
3. لا تترك مجلدًا باسم `build_php` داخل `public_html`.
4. بعد الاستخراج يجب أن تكون البنية الأساسية:

   ```text
   public_html/index.html
   public_html/api/index.php
   public_html/api/container-system.php
   public_html/data/sabaik.db
   public_html/uploads/
   public_html/images/
   public_html/taqi-group-platform/
   public_html/sitemap.xml
   ```

5. لا تدمج الأرشيف فوق ملفات عامة قديمة؛ احذف الملفات العامة القديمة أولًا أو استخدم استبدالًا نظيفًا حتى لا تبقى صفحات HTML أو أصول متقاعدة.
6. تحقق بعد الرفع من:

   ```text
   https://taqigroup.com/
   https://taqigroup.com/sitemap.xml
   https://taqigroup.com/robots.txt
   https://taqigroup.com/taqi-group-platform/
   ```

7. تحقق من أن صورة حالة حاوية حديثة تطلب `.webp`، وأن PNG يبقى متاحًا عند طلبه مباشرة.

## إعادة البناء لاحقًا

```bash
SITE_URL=https://taqigroup.com node scripts/build-hostinger.mjs
pnpm run typecheck
SITE_URL=https://taqigroup.com pnpm --filter @workspace/scripts run seo-quality-gate
pnpm --filter @workspace/scripts run validate:operational
node scripts/test-php-api.mjs
unzip -t taqi-group-hostinger.zip
```

## تحديث لاحق: إصلاح 404 لصفحات SEO

### سبب الخطأ

الروابط العامة التي يولدها الموقع تستخدم convention موحدًا من نوع:

```text
/page/<slug>-<id>
/pages/<slug>-<id>
```

مثل:

```text
/pages/حاويات-مخلفات-البناء-والأنقاض-بالرياض-2
```

كانت طبقة PHP في النسخة المنشورة تبحث عن `slug` المخزن حرفيًا فقط، أو عن تطابق جزئي، ولا تحسب alias الناتج من `slug + id`. لذلك كان طلب `GET /api/pages/<slug>-<id>` يعيد 404 رغم أن السجل موجود في SQLite. الترميز العربي في الرابط لم يكن سبب المشكلة.

### الإصلاح

- تم توحيد مطابقة PHP مع منطق `entitySlug` المستخدم في Node وReact.
- المسار الآن يقبل:
  - `slug` المخزن.
  - `seo_slug` المخزن.
  - alias العام الناتج من `slug` أو `seo_slug` مع رقم السجل.
- بقيت المطابقة مقيدة بالصفحات المنشورة والنشطة فقط.
- تم تحويل `scripts/test-php-api.mjs` إلى اختبار فعلي لجميع صفحات SEO المنشورة، باستخدام alias العام لكل سجل بدل اختبار إداري غير موثّق.

### إثبات الإصلاح

- الرابط الوارد في بلاغ الخطأ، المنتهي بـ`-2`، أعاد HTTP 200 وبيانات الصفحة الصحيحة من `build_php/api/index.php`.
- اختبار PHP الشامل: **PASS (15/15)**.
- فحص الصياغة لنسخة المصدر ونسخة الأرشيف: **PASS**.
- بوابة SEO بعد إعادة البناء: **PASS**.
- الأرشيف الجديد يتضمن الإصلاح داخل `api/index.php`، ولذلك يجب رفعه كحزمة كاملة بدل تعديل ملف PHP منفردًا.

### بيانات الحزمة بعد الإصلاح

- الحجم: `38,351,967` بايت.
- SHA-256:

  ```text
  938b6c1599614474f3a3532cf5444abacb8c84127b4dbb705e529fc67b6d72a4
  ```

- `taqi-group-hostinger.zip` و`cleanflow-services-hostinger.zip` متطابقان.

### المطلوب على Hostinger

ارفع الحزمة الجديدة واستخرجها مباشرة داخل `public_html` بعد أخذ نسخة احتياطية، ولا تكتفِ باستبدال ملفات HTML. يجب أن يصل الإصلاح إلى:

```text
public_html/api/index.php
public_html/data/sabaik.db
```

بعد الرفع أعد فتح أحد روابط `/page/...-id` أو `/pages/...-id` في نافذة خاصة، وتحقق من أن طلب `/api/pages/...-id` يعيد JSON للصفحة بدل 404.