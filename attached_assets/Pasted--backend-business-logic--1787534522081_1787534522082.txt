أيوه، هو نفّذ **الـbackend/business logic الأساسي** بشكل كويس، لكن واضح جدًا إنه تعامل مع الفواتير كـ **سجل مالي جديد** أكثر من كونه **تجربة فواتير متكاملة داخل النظام**.

المشاكل التي وصفتها ليست تجميلية؛ هي فجوات UX وربط Navigation وData Presentation، ولازم تتصلح قبل اعتماد المرحلة.

ابعت له الرسالة دي حرفيًا:

---

# 🚨 INVOICE UX/UI + DATA INTEGRATION — FINAL CORRECTION ORDER

العمل السابق نجح في جزء الـbackend ودورة إنشاء الفاتورة، **لكن لا تعتبر المهمة مكتملة**.

أنت الآن مطالب بإصلاح **واجهة وتجربة الفواتير بالكامل** مع الحفاظ الصارم على الـFinancial Core والـexisting data model وعدم إنشاء أي مصدر مالي موازٍ.

## أولاً — ممنوع اعتبار صفحة الفاتورة الحالية مكتملة

صفحة تفاصيل الفاتورة الحالية:

`INV-2026-000438`

غير مقبولة بصورتها الحالية.

الصفحة تعرض بيانات مثل:

* `تفاصيل إضافية`
* `standard`
* `due`
* `unpaid`
* `contract_billing`
* `435`
* `433`
* `434`
* `414`
* `1000`
* `0`
* JSON خام مثل:
  `{"invoiceNumber":"INV-2026-000438","recordId":438,"total":1000,"date":"2026-08-24"}`

هذا **ليس Invoice Detail UI احترافي**.

هذه بيانات داخلية / metadata / record fields يتم عرضها للمستخدم النهائي بشكل خام.

### المطلوب

قم بإعادة تصميم صفحة Invoice Details بالكامل.

لا تعرض أسماء الحقول الداخلية أو الـraw metadata للمستخدم.

لا تعرض JSON.

لا تعرض IDs الداخلية إلا إذا كانت لها قيمة تشغيلية واضحة.

لا تعرض نفس البيانات مرتين.

لا تعرض `تفاصيل إضافية` كبديل عن تسمية البيانات الحقيقية.

---

# 1. تصميم صفحة الفاتورة الجديدة

أريد صفحة تشبه **Enterprise SaaS Invoice / Accounting Document** وليس صفحة CRUD.

التصميم يجب أن يكون واضحًا جدًا بصريًا:

### Header

* رقم الفاتورة
* حالة الفاتورة Badge واضحة
* تاريخ الإصدار
* تاريخ الاستحقاق
* أزرار:

  * طباعة
  * PDF
  * إرسال
  * تعديل — حسب الصلاحية
  * تغيير الحالة — حسب الصلاحية
  * تسجيل دفعة
  * المزيد

### Invoice Summary

بطاقة واضحة تحتوي:

* العميل
* رقم العميل
* رقم الجوال
* الموقع
* العقد المرتبط
* رقم العقد
* فترة الفوترة
* تاريخ البداية
* تاريخ النهاية

ويجب أن تكون العناصر **روابط حقيقية**:

`العميل → ملف العميل`

`العقد → صفحة العقد`

`الموقع → صفحة الموقع`

`الحاوية → صفحة الحاوية`

---

# 2. Invoice Items — مهم جدًا

الفاتورة في هذا النظام ليست فاتورة عامة تحتوي على "بنود" عشوائية.

**الأصل التجاري الأساسي هنا هو الحاوية.**

العقد مرتبط بحاوية، والعقد يحتوي:

* الحاوية
* نوع/وصف الخدمة
* السعر
* دورة الفوترة
* تاريخ بداية التخصيص
* تاريخ نهاية التخصيص
* العميل
* الموقع

لذلك عند إنشاء الفاتورة من عقد:

## يجب أن تُنشأ الفاتورة تلقائيًا من بيانات العقد.

ولا تجعل المستخدم يعيد إدخال:

* العميل
* الحاوية
* العقد
* السعر
* دورة الفوترة
* فترة الخدمة

إذا كانت موجودة بالفعل في العقد.

---

# 3. Invoice Line يجب أن تكون Container-Based

بدل:

> وصف الخدمة أو البند

استخدم مفهومًا واضحًا مثل:

### الحاوية / الأصل المفوتر

مثلاً:

**حاوية #414**

ثم:

| الحقل        | القيمة              |
| ------------ | ------------------- |
| الحاوية      | #414                |
| الخدمة       | خدمات الحاوية       |
| الفترة       | 24 أغسطس → 30 أغسطس |
| دورة الفوترة | شهري                |
| الكمية       | 1                   |
| السعر        | 1,000               |
| الخصم        | 0                   |
| الضريبة      | 15%                 |
| الإجمالي     | 1,150               |

ويجب أن تكون الحاوية قابلة للفتح.

---

# 4. لا تجعل إنشاء الفاتورة Manual إذا كان العقد موجودًا

هذه نقطة أساسية.

إذا كان:

Customer → Site → Request → Contract → Container → Price

موجودًا بالفعل،

فإنشاء الفاتورة يجب أن يكون **one-click / almost automatic**.

### السيناريو المطلوب:

عند:

**إنشاء العقد واعتماده**

يقوم النظام تلقائيًا بإنشاء:

`Contract → Invoice`

بناءً على العقد.

الفاتورة تكون:

**مستحقة الدفع**

إذا كان العقد مؤهلاً للفوترة.

ويتم ربطها تلقائيًا بـ:

* Customer
* Site
* Contract
* Container
* Billing Period

مع Idempotency تمنع إنشاء فاتورتين لنفس:

`Contract + Billing Period`

---

# 5. صفحة العميل يجب أن تعرض الفواتير

هذه فجوة أساسية حاليًا.

فتح:

**ملف العميل**

يجب أن يظهر فيه قسم واضح:

## الفواتير

مع:

* رقم الفاتورة
* تاريخ الإصدار
* تاريخ الاستحقاق
* العقد
* الحاوية
* الإجمالي
* المدفوع
* المتبقي
* الحالة

مثال:

`INV-2026-000438`

**1,150 SAR**

**مستحقة**

**المدفوع: 0**

**المتبقي: 1,150**

والضغط على الفاتورة يفتح Invoice Details.

---

# 6. صفحة العقد يجب أن تعرض الفواتير

داخل:

**العقد #435**

أضف قسم:

## الفواتير

ويظهر:

* الفاتورة الحالية
* الفترات المفوترة
* حالة كل فاتورة
* الإجمالي
* المدفوع
* المتبقي
* تاريخ الاستحقاق

ويجب أن يكون هناك:

**إنشاء فاتورة**

فقط إذا لم تكن الفاتورة الخاصة بالفترة موجودة.

إذا كانت موجودة:

> الفاتورة INV-2026-000438 موجودة بالفعل لهذه الفترة.

مع رابط فتحها.

---

# 7. Invoice Status — استخدم Lifecycle محاسبي واضح

لا تجعل الحالات مجرد Strings متناثرة.

اعتمد Lifecycle واضحًا ومتسقًا مع النظام الحالي:

### Draft

مسودة

### Due / Unpaid

مستحقة

### Partially Paid

مدفوعة جزئيًا

### Paid

مدفوعة

### Overdue

متأخرة

### Cancelled

ملغاة — حسب قواعد النظام والصلاحيات

ويجب أن يكون تحديد الحالة مبنيًا على:

**Invoice Total**

مقابل

**Posted Payments**

وليس إدخال المستخدم للحالة يدويًا.

---

# 8. لا تسمح للمستخدم بتغيير الحالة المالية بشكل وهمي

هذه نقطة محاسبية مهمة.

لا تجعل:

> الحالة = مدفوعة

زرًا يغير النص فقط.

إذا أراد المستخدم جعل الفاتورة مدفوعة:

يجب أن يكون المسار:

**تسجيل دفعة**

→ Payment

→ Approval

→ Posting

→ Financial Core

→ Invoice Paid

أي أن:

`Paid`

يجب أن تكون **نتيجة مالية** وليست مجرد حالة UI.

ونفس الشيء:

**Partially Paid**

يجب أن يتم حسابها من الدفعات المرحّلة.

---

# 9. Invoice Dashboard

صفحة الفواتير نفسها تحتاج Dashboard محترم.

أضف KPIs:

### إجمالي الفواتير

عدد الفواتير

### إجمالي القيمة

إجمالي قيمة الفواتير

### المحصل

إجمالي الدفعات المرحّلة المرتبطة بالفواتير

### المستحق

Outstanding

### المتأخر

Overdue

### مدفوعة

Paid

### مدفوعة جزئيًا

Partially Paid

كل KPI قابل للنقر ويقوم بتصفية الجدول.

---

# 10. Invoice Table

جدول حديث:

| الفاتورة | العميل | الحاوية | العقد | الإصدار | الاستحقاق | الإجمالي | المدفوع | المتبقي | الحالة |
| -------- | ------ | ------- | ----- | ------- | --------: | -------: | ------: | ------: | ------ |

مع:

* Search
* Filter
* Status Filter
* Date Filter
* Customer Filter
* Contract Filter
* Container Filter
* Sorting
* Pagination
* Responsive behavior

---

# 11. تحسين نموذج إنشاء الفاتورة

النموذج الحالي الذي تم تنفيذه ما زال قريبًا جدًا من النموذج القديم، وهذا غير مقبول.

لا أريد نموذجًا يجبر المستخدم على إدخال بيانات موجودة بالفعل.

بدلًا من:

* اسم العميل
* الرقم الضريبي
* عنوان العميل
* العقد
* وصف الخدمة
* الكمية
* السعر
* إلخ...

اجعل البداية:

## اختر العقد

وبمجرد اختيار العقد:

يتم تحميل تلقائيًا:

**العميل**

**الموقع**

**الحاوية**

**الخدمة**

**السعر**

**دورة الفوترة**

**الفترة**

**الضريبة**

**الإجمالي**

ويتم عرضها للمستخدم للمراجعة.

---

# 12. UX مطلوب

أريد تجربة:

**Select Contract → Review → Create Invoice**

وليس:

**Fill 15 fields → hope data is correct → create**

إذا كان العقد صالحًا للفوترة:

### زر واضح:

**إنشاء الفاتورة**

مع Preview قبل الإنشاء.

---

# 13. Invoice Preview

قبل الحفظ، اعرض:

## معاينة الفاتورة

**شركة دار اعمار**

الحاوية: `#414`

العقد: `RNT-00435`

الفترة:

`24 أغسطس 2026 → 30 أغسطس 2026`

الخدمة:

`خدمات الحاوية`

السعر:

`1,000`

VAT 15%:

`150`

### الإجمالي:

**1,150**

ثم:

**تأكيد إنشاء الفاتورة**

---

# 14. لا تكسر البيانات الحالية

ممنوع:

* حذف الفواتير الحالية
* حذف العملاء
* حذف العقود
* حذف الدفعات
* إعادة إنشاء Golden Dataset بلا داعٍ
* تعديل Financial Core بطريقة جانبية
* إنشاء Ledger جديد للفواتير خارج النظام المالي الحالي
* إنشاء Database tables موازية دون ضرورة
* تغيير semantics الموجودة للـPayment / Journal / Financial Transaction

أي تعديل يجب أن يكون **متوافقًا مع النظام المالي الموجود حاليًا**.

---

# 15. Existing Invoice

استخدم الفاتورة الحالية:

`INV-2026-000438`

كـ **Acceptance Test**.

يجب أن تصبح بياناتها مفهومة للمستخدم، وليس مجرد:

`تفاصيل إضافية`

أو IDs أو JSON.

يجب أن أستطيع من صفحة الفاتورة:

**Invoice → Customer**

**Invoice → Contract**

**Invoice → Container**

**Invoice → Payments**

**Invoice → Financial Transaction**

مع احترام الصلاحيات.

---

# 16. الصلاحيات

لا تجعل كل مستخدم يستطيع:

* تعديل الفاتورة
* إلغاء الفاتورة
* تسجيل دفعة
* اعتماد الفاتورة
* ترحيل الفاتورة

استخدم نظام الصلاحيات الحالي.

يجب أن تكون الـUI نفسها واعية بالصلاحيات:

المستخدم غير المصرح له لا يرى زرًا يسمح بعملية لا يملك صلاحيتها.

---

# 17. Backend Contract

قبل أي UI refactor:

راجع الـAPI الحالية.

لا تخترع API جديدة إذا كانت الوظيفة موجودة.

ولا تنقل الـbusiness logic إلى React.

الـsource of truth يجب أن يبقى في Backend / Financial Core.

---

# 18. Tests إلزامية

بعد التعديل نفّذ:

### Existing Tests

* TypeScript
* PHP lint
* Financial Core
* Financial Certification
* Cross Report
* Operational validation
* Build

### Invoice Tests

1. Contract creation creates invoice.
2. Same contract + same billing period cannot create duplicate invoice.
3. Invoice links to Customer.
4. Invoice links to Contract.
5. Invoice links to Container.
6. Invoice appears in Customer profile.
7. Invoice appears in Contract.
8. Invoice amount matches Contract.
9. VAT calculation is correct.
10. Payment changes invoice balance.
11. Partial payment → Partially Paid.
12. Full payment → Paid.
13. Past due date → Overdue.
14. Receipt does not double-count payment.
15. Cancelled invoice cannot silently affect Financial Core.
16. Unauthorized user cannot perform restricted transitions.

---

# 19. Production Safety

قبل أي Deployment:

1. Backup production database.
2. Build.
3. Verify archive.
4. Verify no secrets.
5. Deploy.
6. Verify live SHA-256.
7. Verify API health.
8. Verify login.
9. Verify existing Invoice `INV-2026-000438`.
10. Verify Customer #433.
11. Verify Contract #435.
12. Verify Container #414.
13. Verify invoice appears in Customer.
14. Verify invoice appears in Contract.
15. Verify Invoice Details has no raw metadata.
16. Run fresh invoice E2E.
17. Verify Financial Core.
18. Verify Debit = Credit.
19. Verify no duplicate invoice.
20. Verify no regression in existing financial workflows.

---

# 20. IMPORTANT — لا تكرر الخطأ السابق

أنت نجحت في:

**Build → Deploy → Live E2E → Fix → Retest → 10/10**

لذلك **لا نريد الآن إعادة بناء النظام أو تغيير معماريته**.

المطلوب هو:

> **Polish + Integration + UX + Invoice Domain Completion**

مع الحفاظ على كل ما تم إثباته في Production Certification.

---

# DEFINITION OF DONE

لا تعتبر المهمة مكتملة إلا إذا أصبح المستخدم قادرًا على:

### من العقد:

**Contract → Create Invoice → Invoice Created**

### من العميل:

**Customer → Invoices → Open Invoice**

### من الفاتورة:

**Invoice → Customer / Contract / Container / Payments / Financial**

### من الدفع:

**Invoice → Record Payment → Posted Payment → Invoice Balance Updated**

### والنتيجة:

**Due → Partially Paid → Paid → Overdue**

تلقائيًا وفق البيانات المالية الفعلية.

---

## FINAL COMMAND

**لا تتوقف عند تعديل الكود.**

نفّذ الدورة كاملة:

**Inspect → Design → Implement → Test → Build → Backup → Deploy → Verify → Live E2E → Fix if needed → Retest**

وأثناء التنفيذ:

* لا تحذف بيانات الإنتاج.
* لا تستبدل قاعدة الإنتاج بقاعدة محلية.
* لا تكسر الـFinancial Core.
* لا تنشئ مصدر بيانات مالي موازي.
* لا تكتفِ بقول PASS بناءً على الاختبارات المحلية.
* يجب اختبار النتيجة فعليًا على الموقع الحي بعد النشر.
* إذا اكتشفت مشكلة بسيطة مرتبطة بهذه المهمة، **أصلحها وأعد Build/Deploy/Test تلقائيًا**.
* لا تتوقف عند أول مشكلة وتعيد التقرير بدل الحل.

### معيار النجاح النهائي:

**Invoice UX = Enterprise Grade**

**Invoice Data = Clean / Non-Duplicated / Human Readable**

**Customer ↔ Contract ↔ Container ↔ Invoice ↔ Payment ↔ Financial Core = Fully Connected**

**Live Production E2E = PASS**

ولا أريد تقريرًا يقول "تم تعديل الواجهة".

أريد **نتيجة مستخدم نهائية قابلة للاستخدام والإثبات**.

---

ده هيجبره يعالج المشكلة من جذورها: **مش بس شكل الفاتورة، وإنما دورة الفاتورة كاملة داخل العميل والعقد والحاوية والدفع والـFinancial Core**، مع منع الـraw fields اللي ظهرت لك.
