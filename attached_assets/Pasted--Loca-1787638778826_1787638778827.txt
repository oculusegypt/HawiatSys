تمام. ابعت له الرسالة دي **كما هي**؛ دي مش رسالة “كمّل شوية”، دي إغلاق مرحلة وإلزامه ينتقل من **Local PASS** إلى **Production E2E حقيقي**، مع منع أي لعب في قاعدة الإنتاج:

---

# 🔴 FINAL COMMAND — PRODUCTION OPERATIONAL + BILLING E2E

لقد أغلقت الآن مرحلة التطوير والاختبار المحلي.

النتائج المحلية الحالية **مقبولة**، لكن لا تعتبر المهمة مكتملة ولا تعتبر النظام Production Certified قبل تنفيذ الدورة الحية كاملة.

## ممنوع اعتبار Local PASS = Production PASS

الـLocal E2E أثبت أن المنطق البرمجي يعمل:

* Work Orders منفصلة عن Service Requests.
* Delivery / Pickup / Emptying تعمل كـWork Orders.
* Driver workflow يعمل.
* Container/Contract synchronization تعمل.
* Invoice → Payment تعمل.
* Partial Payment تعمل.
* Payment Idempotency تعمل.
* Invoice status بعد السداد الجزئي = `partially_paid`.
* Toast موحد.
* Node/PHP parity مقبول محليًا.
* Financial regression PASS.

لكن **هذا ليس نهاية المهمة**.

---

# المرحلة التالية إلزامية: LIVE PRODUCTION E2E

الهدف:

> **DEPLOY → VERIFY → TEST → FIX IF NEEDED → REDEPLOY → RETEST → CERTIFY**

ولا أريد تقريرًا يقول فقط:

> “الأرشيف جاهز لكن FTP blocked”

إلا إذا ثبت فعلًا أن قناة النشر غير قابلة للوصول بعد تجربة جميع المسارات المتاحة.

---

# 1. DEPLOYMENT

استخدم بيانات Hostinger المتوفرة في بيئة المشروع.

المطلوب:

### أولاً Backup

قبل لمس أي ملف:

1. الاتصال بـHostinger.
2. الوصول إلى `/public_html`.
3. أخذ Backup كامل للملفات المطلوبة.
4. تنزيل نسخة قاعدة بيانات الإنتاج الحالية.
5. عدم استبدال `data/sabaik.db` المحلية بقاعدة الإنتاج.
6. الاحتفاظ بنسخة Production DB كما هي.

**ممنوع تمامًا رفع قاعدة البيانات المحلية فوق Production DB.**

---

# 2. DEPLOY PATCH ONLY

ارفع التحديث البرمجي المطلوب فقط.

خصوصًا:

* Frontend build.
* `api/index.php`
* `api/container-system.php`
* الملفات المرتبطة بالـWork Orders.
* ملفات Billing / Payment.
* أي generated API/types مطلوبة.

لا ترفع قاعدة البيانات المحلية.

---

# 3. VERIFY DEPLOYMENT

بعد الرفع لا تفترض أن النشر نجح.

تحقق فعليًا من:

```text
/public_html
/public_html/api
/public_html/index.html
```

وتحقق من الملفات المنشورة.

استخرج SHA-256 للملفات المهمة وقارنها بالنسخة التي تم بناؤها.

يجب أن يكون التقرير:

```text
LOCAL SHA
LIVE SHA
MATCH = PASS
```

وليس مجرد:

```text
Upload completed
```

---

# 4. PRODUCTION HEALTH

بعد النشر مباشرة:

```text
/api/healthz
```

يجب أن تكون:

```json
{
  "php": "ok",
  "database": "ok",
  "application": "ok"
}
```

ثم تحقق من:

* الموقع الرئيسي HTTP 200.
* Login.
* `/api/auth/me`.
* Admin role.
* API authentication.
* `/data/` = 403.
* Database file = 403.
* `.env` = 403.

إذا كسر التحديث أي شيء:

**FIX → BUILD → DEPLOY → VERIFY AGAIN**

لا تتوقف عند تسجيل الخطأ.

---

# 5. PRODUCTION DATABASE INTEGRITY

بعد أخذ نسخة قاعدة الإنتاج:

نفذ عليها:

```sql
PRAGMA quick_check;
PRAGMA integrity_check;
```

والنتيجة المطلوبة:

```text
quick_check = ok
integrity_check = ok
```

ثم تحقق من أن قاعدة الإنتاج تحتوي على بياناتها الحالية ولم يتم استبدالها.

---

# 6. OPERATIONAL LIVE E2E

أنشئ Dataset جديدًا تمامًا باستخدام:

```text
OPERATIONAL-PROD-E2E-2026
```

ولا تستخدم بيانات العملاء الحقيقية للاختبار إن لم يكن ذلك ضروريًا.

نفذ الدورة الكاملة:

### Customer

```text
Customer
↓
Customer Site
↓
Container
↓
Contract
```

ثم:

### Contract → Delivery

إنشاء العقد يجب أن ينتج:

```text
Contract
+
Work Order: Delivery
+
Appointment
```

ولا يجوز إنشاء:

```text
service_request
```

جديد للعملية التشغيلية.

---

# 7. DELIVERY E2E

اختبر:

```text
Work Order
↓
Assign Driver
↓
accepted
↓
started
↓
en_route
↓
arrived
↓
completed
```

وتحقق أن الإكمال يغير:

```text
Contract → delivered
Container → rented
Work Order → completed
```

بدون إنشاء سجل تشغيلي مكرر.

---

# 8. PICKUP / RETURN

نفذ:

```text
Pickup / Return
```

ويجب أن يكون:

```text
Work Order
+
Appointment
```

وليس:

```text
Service Request
```

ثم بعد الإكمال:

```text
Contract → returned
Container → available
Work Order → completed
```

---

# 9. EMPTYING

هذه نقطة حرجة.

اختبر Emptying بشكل مستقل.

**ممنوع أن يعامل Emptying كـDelivery.**

يجب ألا يقوم Emptying تلقائيًا بتغيير:

```text
Contract → delivered
Container → rented
```

إلا إذا كان ذلك مطلوبًا صراحة من حالة العمل الحالية.

تحقق من أن:

```text
Emptying = Work Order
```

وأن حالاته وعلاقاته صحيحة.

---

# 10. IDEMPOTENCY

أعد نفس العملية بنفس:

```text
Idempotency-Key
```

مرتين.

المطلوب:

```text
First Request → CREATE
Second Request → SAME RECORD
```

ولا يجوز إنشاء:

```text
Work Order #1
Work Order #2
```

لنفس العملية.

---

# 11. BILLING LIVE E2E

اختبر دورة الفوترة الحقيقية على Production.

المطلوب:

```text
Customer
↓
Contract
↓
Invoice
```

عند إنشاء العقد:

**يجب إنشاء الفاتورة تلقائيًا طبقًا للعقد.**

الفاتورة يجب أن تكون مرتبطة فعليًا بـ:

```text
Customer
Contract
Container
Billing Period
Amount
```

ولا يوجد إنشاء فاتورة ثانية لنفس العقد والفترة.

---

# 12. CUSTOMER PROFILE

افتح ملف العميل مباشرة.

يجب أن تظهر:

```text
Contracts
Invoices
Payments
Outstanding Balance
```

والفاتورة يجب أن تكون Clickable وتفتح تفاصيلها.

---

# 13. CONTRACT PROFILE

افتح العقد.

يجب أن تظهر:

```text
Invoice
Payment Status
Outstanding Amount
Billing Period
Container
```

والروابط يجب أن تعمل فعليًا.

---

# 14. INVOICE DETAILS

هذه نقطة مهمة جدًا.

لا أريد صفحة تعرض:

```text
تفاصيل إضافية
تفاصيل إضافية
تفاصيل إضافية
[object Object]
435
433
434
...
```

هذا **غير مقبول Production UX**.

يجب تحويل البيانات إلى واجهة بشرية احترافية.

مثلاً:

```text
الفاتورة
INV-2026-XXXX

العميل
شركة ...

العقد
RNT-XXXXX

الحاوية
CNT-XXX

فترة الفوترة
أغسطس 2026

المبلغ قبل الضريبة
...

VAT 15%
...

الإجمالي
...

المدفوع
...

المتبقي
...

الحالة
مستحقة / مدفوعة جزئيًا / مدفوعة / متأخرة
```

ولا يظهر للمستخدم أي:

```text
[object Object]
```

أو أسماء حقول تقنية داخلية.

---

# 15. PAYMENT LIVE E2E

اختبر:

### Payment #1

```text
Invoice = 1000
Payment = 500
```

المطلوب:

```text
Invoice
Status = partially_paid

Paid = 500
Remaining = 500
```

ثم:

### Payment #2

```text
Payment = 100
```

المطلوب:

```text
Paid = 600
Remaining = 400
```

**ممنوع أن يصبح:**

```text
Paid = 1200
```

كما حدث سابقًا.

---

# 16. PAYMENT IDEMPOTENCY

كرر Payment #1 بنفس:

```text
Idempotency-Key
```

المطلوب:

```text
HTTP 200
idempotent = true
Same Payment ID
```

ولا يتغير:

```text
Paid = 600
```

إلى:

```text
Paid = 1100
```

أو:

```text
Paid = 1200
```

---

# 17. OVERPAYMENT

حاول دفع:

```text
Remaining + 1
```

يجب رفض العملية.

لا Payment جديد.

لا Financial Transaction.

لا Journal Entry.

لا تغيير في Invoice balance.

---

# 18. PAYMENT STATUS

تحقق من جميع الحالات:

```text
due
partially_paid
paid
overdue
```

ويجب أن تكون الحالة مشتقة من البيانات المالية الحقيقية، وليس مجرد UI flag.

---

# 19. FINANCIAL CORE

بعد دورة الفوترة والسداد:

تحقق من:

```text
Gross Revenue
Collections
Returned Collections
Net Collections
Expenses
Profit
Cash
Bank
```

ثم:

```text
Total Debit
Total Credit
```

يجب:

```text
Debit == Credit
```

ولا يوجد قيد مالي مكرر بسبب إعادة السداد.

---

# 20. FINANCIAL IMMUTABILITY

تأكد أن:

```text
Payment
```

ينشئ القيد المالي الصحيح مرة واحدة فقط.

إعادة نفس العملية لا تنشئ:

```text
Financial Transaction إضافية
Journal Entry إضافية
```

---

# 21. UI / UX FINAL REVIEW

راجع فعليًا من المتصفح:

### Invoice

* إنشاء سريع.
* اختيار العميل.
* عند اختيار العميل تظهر العقود.
* عند اختيار العقد تظهر الحاوية.
* بيانات العقد تستخدم تلقائيًا.
* الحاوية هي الـInvoice Line Item وليس “بند خدمة” عام.
* السعر يأتي من العقد.
* Billing Period واضح.
* VAT واضح.
* Total واضح.
* لا إعادة إدخال بيانات غير ضرورية.

### Payment

عند الدخول من:

```text
Customer
Contract
Invoice
Payments
```

يجب أن تصل نافذة السداد وهي تعرف تلقائيًا:

```text
Customer
Contract
Invoice
Remaining
Allocation
```

ولا تجبر المستخدم على إعادة اختيار ما تم اختياره بالفعل.

---

# 22. TOAST

كل رسائل:

```text
Success
Error
Warning
Validation
```

يجب أن تظهر:

**أعلى منتصف الشاشة**

وليس:

```text
يمين الشاشة
Browser alert
أسفل الشاشة
```

ويجب ألا تظهر رسالتان متكررتان لنفس العملية.

---

# 23. WORK ORDERS VS REQUESTS

هذا قرار معماري ثابت:

### Service Request

خاص بـ:

```text
طلب العميل / الطلب التجاري
```

### Work Order

خاص بـ:

```text
Delivery
Pickup
Return
Emptying
Operational execution
Driver assignment
Field execution
```

**لا تعكس هذا الفصل في أي مسار جديد.**

---

# 24. NODE / PHP PARITY

أي إصلاح في Node يجب أن يكون له equivalent في PHP Hostinger.

اختبر:

```text
Node behavior
==
PHP behavior
```

خصوصًا:

* Contract → Invoice.
* Payment allocation.
* Payment idempotency.
* Work Order creation.
* Delivery.
* Pickup.
* Emptying.
* Driver transitions.
* Financial posting.

---

# 25. NO PARTIAL IMPLEMENTATION

هذه أهم نقطة:

**لا تعتبر المهمة مكتملة لأن الـbackend يعمل.**

ولا تعتبرها مكتملة لأن:

```text
TypeScript PASS
Build PASS
```

المطلوب:

```text
Backend
+
Frontend
+
Database
+
Financial Core
+
UX
+
Production
+
Live E2E
```

كلها تعمل معًا.

إذا اكتشفت أثناء Live E2E مشكلة صغيرة:

**أصلحها فورًا.**

ثم:

```text
Build
→ Deploy
→ Verify
→ Retest
```

ولا ترجع لي بتقرير “هناك مشكلة” إذا كان بالإمكان إصلاحها ضمن نفس المهمة.

---

# 26. FINAL REPORT

في النهاية أريد تقريرًا بالأرقام، وليس كلامًا عامًا.

استخدم هذا الشكل:

```text
FINAL PRODUCTION CERTIFICATION

DEPLOYMENT: PASS/FAIL
BACKUP: PASS/FAIL
LIVE SHA: PASS/FAIL
DATABASE INTEGRITY: PASS/FAIL

AUTHENTICATION: PASS/FAIL
SECURITY: PASS/FAIL

CUSTOMER → CONTRACT: PASS/FAIL
CONTRACT → WORK ORDER: PASS/FAIL
WORK ORDER → DRIVER: PASS/FAIL
DELIVERY: PASS/FAIL
PICKUP: PASS/FAIL
EMPTYING: PASS/FAIL

CONTRACT → INVOICE: PASS/FAIL
INVOICE IDEMPOTENCY: PASS/FAIL

INVOICE → PAYMENT: PASS/FAIL
PARTIAL PAYMENT: PASS/FAIL
FULL PAYMENT: PASS/FAIL
OVERPAYMENT PROTECTION: PASS/FAIL
PAYMENT IDEMPOTENCY: PASS/FAIL

CUSTOMER INVOICE LINK: PASS/FAIL
CONTRACT INVOICE LINK: PASS/FAIL
INVOICE PAYMENT LINK: PASS/FAIL

INVOICE UX: PASS/FAIL
PAYMENT UX: PASS/FAIL
TOAST SYSTEM: PASS/FAIL

FINANCIAL CORE: PASS/FAIL
DOUBLE ENTRY: PASS/FAIL
NODE/PHP PARITY: PASS/FAIL

REGRESSION SUITE: PASS/FAIL
LIVE E2E: PASS/FAIL
PRODUCTION STABILITY: PASS/FAIL
```

ثم:

```text
FINAL SCORE: X/10
```

**لا تضع 10/10 إلا إذا تم تنفيذ Production Deploy + Backup + Live E2E + SHA + Database Integrity + Financial verification فعليًا.**

---

## 🔴 قاعدة أخيرة

لا أريد:

> “تم تجهيز الأرشيف.”

ولا:

> “Local tests passed.”

ولا:

> “FTP blocked” بدون محاولة حقيقية وتوثيق السبب.

أريد الوصول إلى:

> **COMPLETE LIVE E2E → FIX → DEPLOY → VERIFY → RETEST → CERTIFIED**

إذا كانت هناك مشكلة في قناة النشر، استنفد مسارات الاتصال المتاحة أولًا ووثّق الخطأ بدقة، **ولا تلمس Production DB ولا تستبدلها بالنسخة المحلية تحت أي ظرف.**

**ابدأ الآن من الحالة الحالية، ولا تعِد تنفيذ ما تم إثباته محليًا إلا كـRegression سريع؛ ركّز على إثبات النسخة المنشورة فعليًا وإنهاء الدورة بالكامل.**
