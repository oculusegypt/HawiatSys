أنت Agent جديد ستكمل العمل الذي بدأه Agent سابق على مشروع taqigroup.com.

مهم جدًا: **لا تبدأ من الصفر، ولا تعيد الـAudit الكامل، ولا تعيد قراءة الملفات التي تم فحصها بالفعل إلا إذا احتجت جزءًا محددًا للتحقق.** الهدف هو استكمال العمل من آخر نقطة توقف عندها الـAgent السابق، مع الحفاظ على الـtokens والوقت.

## الحالة التي وصلت إليها من الـAgent السابق

تم تنفيذ 131 إجراء وقراءة حوالي 10,065 سطرًا قبل توقف الـAgent بسبب quota.

الـAgent السابق كان قد:

* فحص التقرير الأصلي للـ14 مشكلة.
* فحص ملفات SEO والـprerender والـHostinger build.
* فحص `App.tsx`.
* فحص `PublicRoutes.tsx`.
* فحص `prerender.mjs`.
* فحص `build-hostinger.mjs`.
* فحص `seo-quality-gate.mjs`.
* فحص `generate-sitemap.mjs`.
* فحص `siteUrl.ts`.
* فحص `SiteSettingsContext.tsx`.
* فحص `useDocumentSEO.ts`.
* فحص `Home.tsx`.
* فحص `ServiceDetail.tsx`.
* فحص `BlogPost.tsx`.
* فحص `NeighborhoodPage.tsx`.
* فحص `AboutPage.tsx`.
* فحص `ContactPage.tsx`.
* فحص `AreasIndexPage.tsx`.
* فحص إعدادات الصور `seoMedia.ts`.
* فحص قاعدة البيانات وبعض بيانات الاتصال.
* فحص بقايا العلامة القديمة.
* فحص Git diff.

## أهم اكتشافات الـAgent السابق

وجد أن هناك فجوات حقيقية لم تكن مغلقة بالكامل:

1. الـHomepage production HTML كان ما زال يستخدم Meta Description بطول 75 حرفًا.
2. بعض أوصاف الصفحات كانت قصيرة.
3. بعض JSON-LD كان يحتوي على provider/entity IDs غير موحدة.
4. كان هناك رقم هاتف قديم داخل البيانات.
5. كانت هناك حاجة لتوحيد قاعدة توليد Meta Description بدل ترقيع الصفحات منفردة.
6. كانت هناك حاجة للتأكد من أن المحتوى الأساسي SEO/GEO موجود في HTML وليس معتمدًا على React hydration فقط.
7. كانت هناك أسماء/ملفات مرتبطة بـCleanFlow تحتاج مراجعة حتى لا تتسرب للـproduction.
8. تم بدء تعديل React وSEO بالفعل.
9. تم بدء تعديل `prerender.mjs` على عدة مراحل.
10. **لكن لم تتم مرحلة البناء النهائي والتحقق النهائي قبل توقف الـAgent.**

## التعديلات التي بدأها بالفعل

تم إنشاء/تعديل:

* `seoText.ts`
* `useDocumentSEO.ts`
* `ServiceDetail.tsx`
* `FloatingContactButtons.tsx`
* `prerender.mjs`

كما بدأ تعديل:

* Meta description normalization.
* توحيد SEO descriptions.
* معالجة provider IDs.
* معالجة أرقام الاتصال القديمة.
* معالجة أسماء ملفات SEO media.
* حماية React mount.
* تقليل الاعتماد على client-side rendering.

**لا ترجع هذه التعديلات للخلف. افحص الـdiff الحالي أولًا ثم أكمل فوقه.**

---

# المطلوب منك الآن

## المرحلة 1 — استئناف آمن

ابدأ بـ:

1. `git diff`
2. حالة الملفات المعدلة.
3. تحقق سريع من التعديلات التي نفذها الـAgent السابق.
4. لا تعيد قراءة المشروع كله.
5. لا تعيد فحص الملفات التي تم فحصها إلا عند الحاجة.

حدد فقط:

* ما تم تطبيقه فعلًا.
* ما بقي غير مكتمل.
* أي تعديل ناقص أو قد يسبب regression.

---

# المرحلة 2 — أكمل Meta SEO

أكمل نظام `seoText.ts` / `useDocumentSEO.ts` / `prerender.mjs`.

المطلوب:

* Homepage description بين 120–160 character تقريبًا.
* أوصاف الصفحات المهمة ضمن نطاق منطقي.
* عدم استخدام نفس Description لكل الصفحات.
* عدم Keyword Stuffing.
* عدم إضافة كلام حشو فقط للوصول إلى عدد أحرف معين.
* الحفاظ على معنى الصفحة.
* تطابق description بين:

  * prerender HTML
  * runtime DOM
  * metadata
  * Schema عند استخدام description.

**مهم:** لا تكتفِ بتغيير SiteSettings؛ أصلح مصدر التوليد نفسه.

---

# المرحلة 3 — Entity Graph

تحقق من JSON-LD الحالي بدل إنشاء نظام آخر.

يجب أن يكون لدينا Entity Graph متسق يربط:

Organization
→ LocalBusiness
→ WebSite
→ WebPage
→ Service
→ ContactPoint
→ AreaServed

استخدم `@id` ثابتًا ومنطقيًا.

أهم شيء:

* لا توجد IDs متكررة لكيانات مختلفة.
* لا توجد Organization مكررة.
* لا توجد LocalBusiness منفصلة عن Organization بدون سبب.
* لا توجد بيانات وهمية.
* لا توجد أرقام قديمة.
* sameAs لا يحتوي روابط غير حقيقية.

---

# المرحلة 4 — البيانات القديمة والأرقام

واصل الفحص الذي بدأه الـAgent السابق.

ابحث عن الأرقام القديمة التي ظهرت أثناء الفحص، خصوصًا:

`0557870222`
`0580595555`

وأي أرقام Placeholder أو fallback غير موثقة.

لكن **لا تحذف رقمًا حقيقيًا لمجرد أنه غير معروف لك**.

القاعدة:

* رقم موثق = يبقى.
* رقم Placeholder/قديم مثبت = يزال.
* رقم غير مؤكد = لا تفترض حالته؛ تحقق من مصدره داخل المشروع.

طبّق ذلك على:

* source
* database
* seed
* JSON-LD
* HTML
* prerender
* build output.

---

# المرحلة 5 — Rendering 1238%

هذه أهم نقطة.

لا تعتبر المشكلة مغلقة بمجرد تعديل React.

تحقق من الفرق بين:

### A

HTML المصدر/SSR/prerender

و

### B

DOM بعد تشغيل JavaScript.

يجب أن تكون المعلومات SEO الأساسية موجودة في HTML قبل hydration:

* Title
* Description
* Canonical
* H1
* النص الرئيسي
* الخدمات
* المناطق
* FAQ
* بيانات الشركة
* الروابط الداخلية
* Schema

لا تحوّل الموقع إلى static HTML كامل إذا كان ذلك سيكسر الوظائف.

الهدف:
**SEO/GEO content = Server/Prerender first**
والـJavaScript يكون للتفاعل والوظائف.

إذا كانت نسبة 1238% ناتجة عن طريقة قياس الأداة نفسها، اشرح ذلك، لكن لا تستخدم هذا كذريعة قبل مقارنة Source وDOM فعليًا.

---

# المرحلة 6 — FAQ / GEO

تحقق أن FAQ موجود فعليًا في HTML وليس مجرد رابط.

يجب وجود أسئلة وأجوبة حقيقية مرتبطة بخدمات تقي.

لا تضف عشرات الأسئلة المصطنعة.

الأولوية:

* السعر.
* أحجام الحاويات.
* مدة التأجير.
* التوصيل والاستبدال.
* نقل الأنقاض.
* المناطق.
* نوع المخلفات.
* طريقة الطلب.

ويجب أن تكون الإجابات واضحة وقابلة للاقتباس من AI Search.

---

# المرحلة 7 — URLs

لا تغير URLs الحالية لمجرد إرضاء SEOptimer.

أولًا حدد الروابط التي اعتبرها التقرير غير Friendly.

إذا كانت جيدة فعليًا:
→ اتركها.

إذا كانت تحتاج تغييرًا حقيقيًا:
→ 301
→ Canonical
→ Internal Links
→ Sitemap
→ أي references أخرى.

لا تعمل mass URL migration.

---

# المرحلة 8 — Analytics / Pixel / Social

لا تنشئ حسابات خارجية ولا بيانات وهمية.

تحقق من الموجود فقط:

### Analytics

إذا يوجد Measurement ID حقيقي:

* تحقق من tracking.
* لا تضف Duplicate Analytics.

إذا لا يوجد:
→ صنف المهمة EXTERNAL CONFIGURATION.

### Facebook Pixel

إذا يوجد Pixel ID حقيقي:
→ تحقق منه.

إذا لا يوجد:
→ EXTERNAL CONFIGURATION.

### YouTube

إذا يوجد حساب رسمي:
→ اربطه.

إذا لا يوجد:
→ EXTERNAL TASK.

### LinkedIn

نفس الشيء.

**لا تعتبر إضافة configuration فارغ إلى الموقع إغلاقًا للمشكلة.**

---

# المرحلة 9 — Google Business Profile

أنا أملك Google account والموقع مضاف ويعمل.

لا تعيد إنشاء Google Business Profile.

تحقق فقط من:

* وجود الرابط/الموقع في البيانات.
* NAP consistency.
* Schema.
* إمكانية اكتشاف العلاقة بين الموقع والكيان.

إذا كانت المشكلة فقط أن SEOptimer لا يستطيع اكتشاف GBP:
→ صنفها DISCOVERY LIMITATION / EXTERNAL VERIFICATION، ولا تعبث بالحساب.

---

# المرحلة 10 — الصور والأداء

التقرير السابق أظهر:

Images ≈ 2.09 MB من 2.60 MB
Image compression = 0%

تحقق من Production output وليس source فقط.

طبّق عند الحاجة:

* WebP/AVIF.
* srcset.
* sizes.
* lazy loading للصور غير الحرجة.
* dimensions.
* عدم تحميل الصور غير المستخدمة.
* عدم وجود legacy images.

لكن لا تحذف صورة SEO مستخدمة فعليًا.

---

# المرحلة 11 — Inline Styles

أكمل إزالة الـInline Styles غير الضرورية.

لا تحاول الوصول إلى صفر بشكل أعمى.

الأولوية:

* styles متكررة.
* styles كبيرة.
* styles يمكن نقلها إلى CSS بسهولة.

ولا تكسر UI.

---

# المرحلة 12 — Legacy Brand Contamination

هذه نقطة مهمة جدًا.

ابحث في Production فقط عن:

* CleanFlow
* sabaik
* سبائك
* الماسة
* أي Branding قديم
* أسماء ملفات قديمة غير لازمة
* URLs قديمة
* Schema قديم
* Database values قديمة

لكن فرّق بين:

1. source/internal tooling الذي لا يظهر للمستخدم.
2. production/public output الذي يمكن أن يصل لمحركات البحث.

لا تعيد تسمية ملفات داخلية بلا داعٍ إذا كان ذلك سيستهلك وقتًا أو يسبب regression.

المهم ألا تتسرب العلامات القديمة إلى:
HTML / metadata / JSON-LD / sitemap / robots / llms.txt / public assets / user-visible content.

---

# المرحلة 13 — Build

بعد الانتهاء:

شغّل فقط الاختبارات الضرورية:

* typecheck
* production build
* PHP syntax
* SQLite integrity
* sitemap validation
* HTML SEO gate
* archive integrity

ولا تعيد اختبارات مكلفة تم تشغيلها بالفعل إذا لم يتغير الجزء المرتبط بها.

---

# المرحلة 14 — Production Package

أعد بناء Hostinger package بعد اكتمال الإصلاحات.

لكن **لا تعتبر ZIP جاهزًا لمجرد أنه unzip صحيح**.

افحص داخله:

* index.html
* sitemap.xml
* robots.txt
* llms.txt
* JSON-LD
* HTML metadata
* public images
* database
* API
* absence of legacy branding.

---

# الأهم: لا تدّعي إغلاق المشاكل الخارجية

في النهاية أريد Matrix صريحة:

| #  | المشكلة                 | الحالة                  |
| -- | ----------------------- | ----------------------- |
| 1  | Backlinks               | CLOSED / EXTERNAL       |
| 2  | Meta Description        | CLOSED / OPEN           |
| 3  | Identity Schema         | CLOSED / OPEN           |
| 4  | Friendly URLs           | CLOSED / N/A            |
| 5  | Google Business Profile | CLOSED / EXTERNAL       |
| 6  | Rendering 1238%         | CLOSED / PARTIAL / OPEN |
| 7  | FAQ/Q&A                 | CLOSED / OPEN           |
| 8  | YouTube                 | CLOSED / EXTERNAL       |
| 9  | LinkedIn                | CLOSED / EXTERNAL       |
| 10 | Analytics               | CLOSED / EXTERNAL       |
| 11 | Address/NAP             | CLOSED / OPEN           |
| 12 | LocalBusiness Schema    | CLOSED / OPEN           |
| 13 | Facebook Pixel          | CLOSED / EXTERNAL       |
| 14 | Inline Styles + Images  | CLOSED / PARTIAL        |

## تعريف CLOSED

لا تستخدم CLOSED إلا إذا:

**تم الإصلاح + تم التحقق من النتيجة.**

Build PASS وحده ليس دليلًا على SEO closure.

---

# توفير الـTokens

مهم جدًا:

لا ترسل لي شرحًا طويلًا أثناء التنفيذ.

نفّذ العمل أولًا.

لا تعيد:

* التقرير الأصلي كاملًا.
* الـAudit الكامل.
* قراءة 10,000 سطر مرة أخرى.
* إنشاء ملفات توثيق كثيرة قبل إنهاء الإصلاح.

استخدم نتائج الـAgent السابق كـbaseline.

ابدأ من الـgit diff الحالي.

ثم:
**Fix → Build → Verify → Closure Matrix**

وفي النهاية أعطني فقط:

1. الملفات التي تغيرت.
2. المشاكل الـ14 وحالة كل واحدة.
3. الأدلة الرقمية المهمة.
4. الاختبارات التي نجحت.
5. ما بقي EXTERNAL فقط.
6. مسار/اسم حزمة الإنتاج النهائية إذا تم بناؤها.

لا تعيد بناء ما تم إنجازه بالفعل إلا إذا اكتشفت أثناء Verification أنه غير صحيح.
