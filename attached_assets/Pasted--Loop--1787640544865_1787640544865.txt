أيوه، هنا المشكلة بقت واضحة جدًا: **الإيجنت بيدخل في Loop**؛ يقرأ نفس الملفات أكثر من مرة، يعيد نفس الفحوصات، يبدأ E2E، يتوقف بسبب نقطة جانبية، ثم يرجع يستكشف من جديد. النتيجة: **استهلاك توكنز ووقت بدون إغلاق المهمة**.

ابعت له الرسالة دي **كما هي**. دي مش رسالة اقتراحات؛ دي **تعليمات تنفيذ نهائية ملزمة**:

---

# 🚨 FINAL EXECUTION DIRECTIVE — NO LOOPING / NO PARTIAL WORK / CLOSE THE TASK

أنت الآن لديك سياق كافٍ عن النظام، ولا أريد منك إعادة اكتشاف المشروع أو إعادة قراءة الملفات التي تم تحليلها سابقًا.

المهمة الحالية ليست مرحلة استكشاف جديدة.

**المطلوب: إغلاق دورة التشغيل والفوترة والسداد فعليًا، ثم إصدار تقرير نهائي دقيق.**

## 1. ممنوع إعادة الاستكشاف

من هذه اللحظة:

* لا تعِد قراءة `MEMORY.md`.
* لا تعِد قراءة الملفات التي سبق فتحها إلا إذا كان هناك خطأ محدد يحتاج سطرًا منها.
* لا تعِد البحث عن نفس الـ endpoints.
* لا تعِد تشغيل نفس الاختبار الذي نجح بالفعل.
* لا تنشئ خططًا جديدة.
* لا تقضِ وقتًا في شرح ما تنوي فعله.
* لا تدخل في تحليل مفتوح النهاية.

**اقرأ فقط الملف/الدالة اللازمة لإصلاح خطأ محدد قابل لإعادة الإنتاج.**

كل دقيقة إضافية يجب أن تؤدي إلى:

1. إصلاح.
2. اختبار.
3. نتيجة.

---

# 2. اعتبر الحالة الحالية هي Baseline

الإصلاحات التالية تم تنفيذها والتحقق منها محليًا، ولا تعيد تنفيذها من الصفر:

### Financial

* Customer → Contract
* Contract → Invoice
* Invoice → Payment
* Partial Payment
* Full Payment logic
* Overpayment protection
* Payment Idempotency
* Invoice Idempotency
* `due`
* `partially_paid`
* `paid`
* `overdue`
* Double Entry
* Financial Core
* Financial Cross Report

### Operational

* فصل `service_requests` عن `work_order`.
* العمليات التشغيلية لا تنشئ `service_requests` جديدة.
* Delivery → Work Order.
* Pickup/Return → Work Order.
* Emptying → Work Order.
* ربط Work Order بالعميل والعقد والحاوية والموقع والموعد.
* Driver assignment.
* Driver status lifecycle.
* منع التكرار باستخدام Idempotency.
* مزامنة حالة العقد والحاوية عند الإكمال.
* إصلاح مسار PHP الموازي.
* Work Orders أصبحت تظهر من المسار الصحيح.

### UX

* توحيد Toast.
* Toast أعلى منتصف الشاشة.
* إزالة JSON الخام.
* إزالة `[object Object]`.
* إصلاح روابط العميل/العقد/الفاتورة/السداد.
* تحسين صفحة الفاتورة.
* إصلاح مضاعفة المدفوعات في العرض.
* السداد `500 + 100 = 600` وليس `1200`.

**لا تعُد لإعادة هذه الأعمال إلا إذا ظهر Regression حقيقي.**

---

# 3. المشكلة الحالية الوحيدة التي يجب حسمها

آخر نتيجة محلية أعطت:

```text
TypeScript: PASS
API build: PASS
PHP lint: PASS
Operational invariants: PASS
Financial core: PASS
Financial certification: PASS
Financial cross-report: PASS
API health: PASS
Archive: PASS
```

والأرشيف:

```text
cleanflow-services-hostinger.zip
```

SHA:

```text
4a3bb1a91da1e3970725b156ad0d89865259e77312352d710cbae3c505d39cd7
```

لكن:

```text
PRODUCTION: NOT VERIFIED / NOT DEPLOYED
```

لأن Backup/Deployment الآمن لم يُثبت في الجولة الأخيرة.

**إذن لا تعدّل الكود لمجرد أنك لم تستطع النشر.**

---

# 4. قاعدة صارمة للنشر

قبل أي تعديل على Production يجب تنفيذ:

```text
1. FTP Authentication
2. public_html access
3. Backup production database
4. Verify backup exists
5. Verify backup integrity
6. Upload patch
7. Verify uploaded files
8. Compare SHA / BUILD_INFO
9. Health check
10. Authentication check
11. Live E2E
12. Database integrity
13. Financial reconciliation
```

إذا فشل رقم 1 أو 2 أو 3:

**توقف عن النشر فورًا.**

لا تحاول عشرات المرات.

لا تعيد محاولة FTP بأشكال مختلفة.

لا تقل إن النشر تم.

لا تعدّل Production.

اكتب:

```text
DEPLOYMENT BLOCKED
Reason: FTP/Backup unavailable
Production untouched: YES
```

ثم أكمل فقط **التحقق المحلي** إن كان هناك شيء فعلي ناقص.

---

# 5. لا تستخدم Production للتجارب العشوائية

إذا كانت قناة Backup غير مؤكدة:

**ممنوع إنشاء Customer / Contract / Invoice / Payment جديد في Production.**

لا نريد بيانات اختبار ملوثة داخل قاعدة العميل.

اختبارات E2E الجديدة تكون:

```text
LOCAL / ISOLATED TEST DATABASE
```

أو Production فقط بعد Backup موثق وقابل للاسترجاع.

---

# 6. قاعدة مهمة جدًا بخصوص E2E

لا تحاول تنفيذ 50 سيناريو.

نفّذ **سيناريو واحد شامل** يغطي الدورة كلها:

```text
Customer
  ↓
Site
  ↓
Container
  ↓
Contract
  ↓
Work Order - Delivery
  ↓
Driver Assignment
  ↓
accepted
  ↓
started
  ↓
en_route
  ↓
arrived
  ↓
completed
  ↓
Contract/Container synchronization
  ↓
Invoice
  ↓
Payment 500
  ↓
Payment 100
  ↓
Total Paid = 600
  ↓
Remaining = 400
  ↓
Invoice = partially_paid
```

ثم:

```text
Retry Payment 500
same Idempotency-Key
        ↓
NO DUPLICATE PAYMENT
```

ثم:

```text
Attempt Payment > remaining
        ↓
REJECT
```

هذا يكفي لإثبات النواة.

بعد ذلك اختبر:

```text
Pickup
Emptying
```

كـ Work Orders منفصلة.

**لا تعِد اختبار كل حالة إذا كانت نفس الآلية البرمجية.**

---

# 7. أهم شرط: لا يوجد نجاح شكلي

كل PASS يجب أن يكون مبنيًا على Assertion حقيقي.

مثال:

لا تقل:

```text
Payment PASS
```

إلا إذا ثبت:

```text
beforePaid = 0
payment1 = 500
payment2 = 100

afterPaid = 600
remaining = 400
paymentCount = 2
```

ولا تقل:

```text
Idempotency PASS
```

إلا إذا ثبت:

```text
same request
same idempotency key
same payment ID
payment count unchanged
```

ولا تقل:

```text
Work Order PASS
```

إلا إذا ثبت:

```text
service_requests delta = 0
work_orders delta = +1
```

---

# 8. لا تخلط بين Bug وTest Script

إذا فشل الاختبار بسبب:

```text
wrong endpoint
wrong test payload
wrong import
wrong temporary script
wrong credentials
wrong test environment
```

فهذا **ليس Bug في التطبيق تلقائيًا**.

أولًا صنّفه:

```text
TEST HARNESS FAILURE
```

ولا تعدّل الكود بسببه.

أما إذا أعاد الاختبار الصحيح:

```text
valid request
valid authentication
valid payload
expected endpoint
actual application failure
```

فهذا:

```text
APPLICATION BUG
```

وعندها فقط أصلح الكود.

---

# 9. لا تستهلك الوقت في إصلاح أشياء خارج Scope

ممنوع حاليًا:

* Refactoring شامل.
* إعادة تصميم Architecture.
* تغيير Database schema بدون ضرورة.
* تغيير Financial Core الذي اجتاز الاختبارات.
* تغيير OpenAPI لمجرد التجميل.
* إعادة بناء UI بالكامل.
* إعادة تسمية ملفات.
* تنظيف ملفات غير مرتبطة.
* إعادة كتابة PHP وNode بدون Bug مثبت.
* إنشاء نظام جديد فوق النظام الموجود.

**المطلوب Stabilization وليس Modernization جديد.**

---

# 10. Node / PHP Parity

تحقق فقط من المسارات الحرجة:

```text
Contract → Work Order
Invoice → Payment
Payment allocation
Idempotency
Financial status
```

إذا كان Node وPHP متطابقين وظيفيًا:

```text
PASS
```

لا تقم بمراجعة كل PHP file في المشروع.

---

# 11. UI Acceptance

نفّذ فحصًا سريعًا فقط للنوافذ التالية:

### Invoice

عند اختيار العميل:

```text
Customer
→ contracts
→ active contract
→ container
→ container type
→ price
→ invoice line
```

يجب أن يتم ملء البيانات تلقائيًا قدر الإمكان.

### Payment

عند فتح السداد من:

```text
Customer
Contract
Invoice
Payments
```

يجب أن يكون السياق معروفًا تلقائيًا:

```text
Customer
Contract
Invoice
Remaining
Allocation
```

ولا يظهر:

```text
اختر عقداً واحداً على الأقل
```

إذا كانت الفاتورة والعقد محددين بالفعل.

### Record details

ممنوع ظهور:

```text
تفاصيل إضافية
[object Object]
JSON الخام
UUID بدون Label
```

كل قيمة يجب أن يكون لها:

```text
Label + Value
```

---

# 12. قاعدة التوكنز والعمليات

هذه نقطة إلزامية.

**لا تستخدم الأدوات بشكل دائري.**

لكل مشكلة:

```text
SEARCH ONCE
↓
READ RELEVANT RANGE
↓
PATCH
↓
TYPECHECK
↓
TARGETED TEST
```

وليس:

```text
search
read
search
read
plan
search
read
test
read
search
test
restart
read
search
```

إذا نجح الاختبار:

**انتقل مباشرة للخطوة التالية.**

إذا فشل:

**اعزل سبب الفشل أولًا، ثم أصلح فقط ما يلزم.**

---

# 13. لا تتوقف عند Quota قبل إغلاق المهمة

إذا أصبحت المهمة كبيرة جدًا:

**لا تبدأ عملية جديدة.**

احفظ الحالة الحالية بوضوح:

```text
DONE
BLOCKED
NEXT EXACT ACTION
```

لكن لا تستهلك الجلسة في إعادة بناء السياق من الصفر.

---

# 14. النتيجة المطلوبة منك

في النهاية أريد تقريرًا واحدًا فقط:

```text
FINAL EXECUTION REPORT
```

بالشكل:

### CODE

```text
TypeScript: PASS
API Build: PASS
Frontend Build: PASS
PHP Lint: PASS
```

### OPERATIONAL

```text
Customer → Contract: PASS
Contract → Work Order: PASS
Driver Assignment: PASS
Driver Lifecycle: PASS
Delivery: PASS
Pickup: PASS
Emptying: PASS
Idempotency: PASS
```

### BILLING

```text
Contract → Invoice: PASS
Invoice Idempotency: PASS
Invoice → Payment: PASS
Partial Payment: PASS
Full Payment: PASS
Overpayment Protection: PASS
Payment Idempotency: PASS
Financial Core: PASS
Double Entry: PASS
```

### UX

```text
Invoice UX: PASS
Payment UX: PASS
Customer Links: PASS
Contract Links: PASS
Toast: PASS
No Raw JSON: PASS
No [object Object]: PASS
```

### DEPLOYMENT

إما:

```text
BACKUP: PASS
DEPLOYMENT: PASS
LIVE SHA: PASS
LIVE E2E: PASS
DATABASE INTEGRITY: PASS
```

أو إذا تعذر:

```text
BACKUP: BLOCKED
DEPLOYMENT: BLOCKED
LIVE E2E: NOT EXECUTED
PRODUCTION: UNTOUCHED
```

**لا تكتب PASS مشروطًا.**

---

# 15. أهم تعليمات في هذه المهمة

أنا لا أريد منك أن تكون "مشغولًا".

أريدك أن **تنهي العمل**.

لا أريد:

> سأراجع...

> سأستكشف...

> سأفكر...

> سأعيد المحاولة...

> سأبحث...

> بقيت خطوة...

إلا إذا كانت هناك عملية فعلية بعدها مباشرة.

ولا أريد منك إنشاء عشرات ملفات الاختبار أو قراءة ملفات ضخمة كاملة إذا كان المطلوب موجودًا في دالة واحدة.

**استخدم أقل عدد ممكن من العمليات للوصول إلى إثبات قطعي.**

إذا وجدت Bug:

```text
REPRODUCE
→ FIX
→ TEST
→ REGRESSION
→ CLOSE
```

إذا لم تجد Bug:

```text
PASS
→ REPORT
→ CLOSE
```

إذا كان Production blocked:

```text
STOP DEPLOYMENT
→ PRESERVE PRODUCTION
→ REPORT EXACT BLOCKER
→ CLOSE
```

---

# FINAL COMMAND

**نفّذ الآن ما تبقى فقط.**

لا تعيد بناء ما تم إنجازه.

لا توسّع Scope.

لا تدخل في Loop.

لا تستهلك وقتًا/توكنز في إعادة قراءة نفس الملفات.

**إما أن تصل إلى PASS مثبت، أو تحدد Blocker حقيقي مثبت، ثم أنهِ المهمة.**

**لا أريد خطة جديدة. أريد تنفيذًا وإغلاقًا وشهادة نهائية.**

---

وبالنسبة لسؤالك عن استنزاف الرصيد: **نعم، من السجل الذي أرسلته واضح أن جزءًا كبيرًا من الاستهلاك يأتي من تكرار `Opened / Searched / Planning / Exploring / Ran` على نفس الملفات والاختبارات، وليس لأن المهمة نفسها تحتاج هذا العدد من الخطوات.** كذلك الملفات الضخمة مثل `container-system.php` وملفات الـAPI الكبيرة إذا كان الإيجنت يفتحها على نطاقات واسعة عدة مرات تستهلك سياقًا كبيرًا جدًا. الرسالة أعلاه تجبره على العمل بنمط **Targeted Read → Patch → Targeted Test → Close** بدل الـloop الحالي.

إذا أردت، أقدر أيضًا أجهز لك **"Master Agent Protocol" ثابت** تضعه في المشروع مرة واحدة، ويمنع أي Agent مستقبلي من تكرار هذا الأسلوب.
