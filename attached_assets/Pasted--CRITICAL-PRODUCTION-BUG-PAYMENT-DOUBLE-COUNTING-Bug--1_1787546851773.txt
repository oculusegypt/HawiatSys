🚨 CRITICAL PRODUCTION BUG — PAYMENT DOUBLE COUNTING

وجدت الآن Bug مالي حقيقي في النسخة الحية، ويجب إيقاف أي Certification فورًا حتى يتم إصلاحه وإعادة اختبار الدورة كاملة.

البيانات الحية الحالية تثبت المشكلة:

Invoice:
INV-2026-000438

Invoice Total:
1,000 SAR

Payments:
PAY-... = 500 SAR
PAY-... = 100 SAR

REAL TOTAL PAID:
600 SAR

REAL REMAINING:
400 SAR

لكن النظام يعرض:

Paid:
1,200 SAR

Remaining:
0 SAR

Status:
paid

وفي ملف العميل يظهر أيضًا:

إجمالي المطالبات:
1,000 SAR

المدفوع:
600 SAR

الرصيد المستحق:
400 SAR

بينما بطاقة الفاتورة نفسها تعرض:

1,000 SAR (1,200 SAR)
0 SAR · مدفوعة

إذن لدينا تناقض مالي بين Customer Profile وInvoice Details.

هذا Bug محاسبي CRITICAL وليس UI-only.

━━━━━━━━━━━━━━━━━━━━
1. STOP CERTIFICATION
━━━━━━━━━━━━━━━━━━━━

ممنوع إعلان:

PASS
10/10
Production Certified

حتى يتم تحديد مصدر الـ1,200 وإصلاحه.

لا تقم بحذف السجلات الحالية.

لا تقم بإنشاء بيانات عشوائية جديدة لإخفاء المشكلة.

لا تقم بتعديل الأرقام يدويًا في قاعدة البيانات.

احتفظ بالسجلات الحالية كدليل على الـBug.

━━━━━━━━━━━━━━━━━━━━
2. TRACE THE PAYMENT CALCULATION
━━━━━━━━━━━━━━━━━━━━

افحص المسار بالكامل:

Payment
→ Allocation
→ Invoice
→ Contract
→ Customer
→ Financial Transaction
→ Journal Entry
→ Invoice paid
→ Customer paid
→ Remaining
→ Invoice status

حدد بالضبط لماذا:

500 + 100 = 600

يتم تحويلها إلى:

1,200

أريد تحديد الـsource الحقيقي للـdouble counting.

افحص خصوصًا:

- payment.amount
- allocations[].amount
- invoice.paid
- contract.paid
- customer.paid
- financial_transactions
- journal entries
- receipt/payment linkage
- أي fallback أو aggregation ثانوي
- أي استخدام للـPayment + Allocation معًا في نفس SUM
- أي جمع للـReceipt باعتباره Payment
- أي duplicate join بين Payment وAllocation
- أي duplicate بسبب ربط Contract + Invoice
- أي cached/derived paid value لا يتم تحديثه بشكل صحيح

━━━━━━━━━━━━━━━━━━━━
3. IMPORTANT ACCOUNTING RULE
━━━━━━━━━━━━━━━━━━━━

القاعدة الصحيحة:

Invoice Total = 1,000

Payment #1 = 500
Payment #2 = 100

Paid = 600
Remaining = 400

ولا يجوز أن يتم احتساب Allocation نفسها مرة ثانية كأنها Payment مستقل.

يجب تحديد مصدر Canonical واحد للـpaid amount.

مثال:

SUM(posted Payments allocated to Invoice)

وليس:

SUM(Payments)
+
SUM(Allocations)

إذا كانت Allocation جزءًا من Payment أصلًا.

━━━━━━━━━━━━━━━━━━━━
4. REQUIRED INVARIANTS
━━━━━━━━━━━━━━━━━━━━

يجب أن يثبت النظام دائمًا:

paid = SUM(valid posted payment allocations)

remaining = max(invoice.total - paid, 0)

paid <= invoice.total

إذا:

paid = 0
→ due

0 < paid < total
→ partially_paid

paid >= total
→ paid

وإذا تجاوز السداد الإجمالي:

→ REJECT

ولا يتم إنشاء Payment أو Financial Transaction أو Journal إضافي.

━━━━━━━━━━━━━━━━━━━━
5. DO NOT "FIX" THE DISPLAY ONLY
━━━━━━━━━━━━━━━━━━━━

ممنوع إصلاح Invoice UI فقط.

يجب إصلاح المصدر المشترك الذي تعتمد عليه:

Customer Profile
Invoice Details
Contract
Payment
Financial Core
Reports

بحيث تستخدم كلها نفس الحساب الصحيح.

بعد الإصلاح يجب أن تعرض كل الأماكن:

Invoice:
1,000

Paid:
600

Remaining:
400

Status:
partially_paid

Customer:
Claims = 1,000
Paid = 600
Due = 400

━━━━━━━━━━━━━━━━━━━━
6. PAYMENT DETAILS PAGE IS ALSO BROKEN
━━━━━━━━━━━━━━━━━━━━

صفحة Payment الحالية تعرض:

"تفاصيل إضافية"

وتعرض:

[object Object]

وهذا غير مقبول في Production.

يجب إعادة تصميم Payment Details بحيث لا تعرض raw JSON أو object serialization.

بدل:

تفاصيل إضافية
[object Object]

اعرض بيانات مفهومة:

رقم السداد
العميل
العقد
الفاتورة
المبلغ
طريقة الدفع
تاريخ السداد
الحالة
المبلغ الموزع
الرصيد قبل السداد
الرصيد بعد السداد
مرجع العملية
القيد المالي
الحاوية / الخدمة إن وجدت

وأي metadata إضافية يجب تحويلها إلى labels وقيم مفهومة.

لا يظهر للمستخدم:

[object Object]

ولا:

recordId
UUID
raw JSON
internal keys

إلا إذا كانت شاشة تقنية مخصصة للإدارة.

━━━━━━━━━━━━━━━━━━━━
7. FIX ALL PAYMENT ENTRY POINTS
━━━━━━━━━━━━━━━━━━━━

الإصلاح يجب أن يعمل من:

Customer Profile
Contract
Invoice Details
Payments Screen
Financial Workspace
أي نافذة Payment أخرى

ولا أريد مسارًا واحدًا يستخدم logic مختلفًا.

يجب أن يكون هناك Payment workflow موحد.

━━━━━━━━━━━━━━━━━━━━
8. TEST WITH CURRENT DATA FIRST
━━━━━━━━━━━━━━━━━━━━

قبل إنشاء أي بيانات جديدة:

استخدم البيانات الحالية كـRegression Evidence:

Invoice:
INV-2026-000438

Payments:
500
100

أثبت أن الحساب الحالي:

WRONG:
1,200

ثم بعد الإصلاح:

CORRECT:
600

Remaining:
400

Status:
partially_paid

━━━━━━━━━━━━━━━━━━━━
9. THEN CREATE A NEW FRESH DATASET
━━━━━━━━━━━━━━━━━━━━

بعد إصلاح الـBug أنشئ Dataset جديد:

HOSTINGER-PAYMENT-DOUBLECOUNT-FIX-2026

واختبر:

Invoice = 1,000

Payment #1 = 500
Payment #2 = 100

Expected:

Paid = 600
Remaining = 400

ثم:

Payment #3 = 400

Expected:

Paid = 1,000
Remaining = 0
Status = paid

ثم حاول:

Payment #4 = 1

Expected:

HTTP 4xx rejection

No Payment
No Financial Transaction
No Journal Entry

━━━━━━━━━━━━━━━━━━━━
10. IDEMPOTENCY
━━━━━━━━━━━━━━━━━━━━

كرر Payment #1 بنفس Idempotency-Key.

يجب أن يعيد:

same Payment ID
same allocation
same financial transaction
same journal

ولا يتغير:

Paid

بأي مقدار إضافي.

أي إعادة إرسال لنفس العملية يجب ألا تؤثر على الرصيد.

━━━━━━━━━━━━━━━━━━━━
11. DATABASE / FINANCIAL CHECK
━━━━━━━━━━━━━━━━━━━━

بعد الإصلاح تحقق من:

Total Debit
Total Credit

ويجب:

Debit = Credit

Difference = 0

وتحقق أيضًا من عدم وجود duplicate financial transactions أو journals بسبب السداد المكرر.

━━━━━━━━━━━━━━━━━━━━
12. UI REGRESSION
━━━━━━━━━━━━━━━━━━━━

راجع:

Customer Profile
Invoice List
Invoice Details
Payment List
Payment Details
Contract Details
Financial Core
Reports

ويجب أن تظهر نفس الأرقام في كل مكان.

لا يجوز أن يظهر:

Customer:
600 paid

بينما Invoice:
1,200 paid

━━━━━━━━━━━━━━━━━━━━
13. DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━

بعد إصلاح الكود:

Local:
TypeScript PASS
PHP lint PASS
Frontend build PASS
API build PASS
Financial tests PASS

ثم:

BACKUP
→ DEPLOY CODE ONLY
→ DO NOT REPLACE PRODUCTION DB
→ LIVE HEALTH
→ LIVE LOGIN
→ LIVE PAYMENT TEST
→ VERIFY

━━━━━━━━━━━━━━━━━━━━
14. PRODUCTION DATA SAFETY
━━━━━━━━━━━━━━━━━━━━

ممنوع:

- حذف Payment
- حذف Invoice
- تعديل أرقام Payment يدويًا
- استبدال SQLite production
- إعادة بناء قاعدة البيانات
- إسقاط records

أصلح الـcalculation logic.

إذا احتاجت البيانات الحالية إلى correction بسبب mutation حدثت أثناء الاختبار، لا تقم بأي correction يدوي قبل تحديد مصدر الـdouble counting وتوثيق السبب.

━━━━━━━━━━━━━━━━━━━━
15. FINAL ACCEPTANCE
━━━━━━━━━━━━━━━━━━━━

لن تعتبر المهمة ناجحة إلا إذا أصبح:

Invoice:
1,000

Payment 1:
500

Payment 2:
100

Paid:
600

Remaining:
400

Status:
partially_paid

ثم بعد دفع 400:

Paid:
1,000

Remaining:
0

Status:
paid

ثم محاولة دفع 1:

REJECTED

ولا توجد mutation مالية.

━━━━━━━━━━━━━━━━━━━━
16. FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

في التقرير النهائي أريد:

1. Root Cause
2. Exact Code Path Causing Double Count
3. Fix Applied
4. Current Production Record IDs
5. Before/After Values
6. Regression Tests
7. Payment Idempotency Result
8. Overpayment Result
9. Financial Core Result
10. Debit/Credit Result
11. UI Result
12. Deployment Result
13. Live Retest Result

والأهم:

لا تكتب PASS لمجرد أن الـAPI يستجيب.

المعيار هو:

500 + 100 = 600

وليس 1,200.

هذه مشكلة محاسبية، لذلك تعامل معها كـP0 Financial Integrity Bug.

ابدأ بتحليل مصدر الـ1,200، أصلح السبب الجذري، ثم اختبر الدورة كاملة مرة أخرى.