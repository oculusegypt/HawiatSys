Planning an effective strategy

I think we need to create a durable plan file. The build mode shouldn’t focus on project tasks but rather on documenting the plan directly. First, I’ll evaluate the code broadly using the shell, and I might need to use a subagent for that. It seems useful to explore the app route list and its components to get a more comprehensive view. I'll aim to execute everything in parallel as instructed.

سأتعامل معها كبرنامج تطوير شامل على مراحل: أبدأ بتقييم النظام الحالي بالكامل وتحديد نقاط التشتت والمخاطر، ثم أضع خطة تنفيذ عملية، وبعدها أبدأ مباشرة بأكثر التحسينات تأثيرًا على تجربة المستخدم بدل إجراء تغييرات عشوائية على النوافذ. سأحافظ على الوظائف الأساسية وأبسط الوصول إليها دون حذف قدرات النظام.



Show less

Completed product surface audit

Subagent: Audit the existing CleanFlow Services and CleanFlow Platform apps. Map all routes/pages, major windows/forms, repeated navigation patterns, and the biggest user confusion points. Read-only; return concise findings with exact file paths.
Task
Audit the existing CleanFlow Services and CleanFlow Platform apps. Map all routes/pages, major windows/forms, repeated navigation patterns, and the biggest user confusion points. Read-only; return concise findings with exact file paths.

29 actions
Working
Result
CleanFlow Services (artifacts/sabaik-almasa)
Route/page map
App-level routing/auth: src/App.tsx:27-47 — /admin/login, /admin/requests/:id/print, all /admin/*, then public fallback; RTL Arabic is forced at src/App.tsx:95-100.
Public routes: src/routes/PublicRoutes.tsx:31-108:
/ Home; /services also renders Home (:64-65), which makes “services” a section alias rather than a page.
Blog: /blog, /blog/:slug, Arabic /المدونة, /المدونة/:slug; legacy /news redirects to blog (:32-39).
About/contact/FAQ/privacy/terms each have English, trailing-slash and Arabic aliases (:41-59).
Partners /partners; pricing /pricing; services /services/:slug plus /خدماتنا/:slug; container/package catalog has /cleaning-packages, /packages, /containers and category/detail variants including /containers/{debris,waste,contracts,contract}/, /container/:slug, /حاويات/:category (:61-86).
Areas index and neighborhood detail: /areas, /areas/:slug, Arabic aliases (:88-93).
Dynamic SEO CMS pages: /page(s)/:slug, Arabic /صفحة(s)/:slug (:95-100). Why-us subpages /why-us/{leadership,what-we-do,commitment,experience} and standalone /chat (:102-107). Unknown routes use not-found (:108).
Admin route map: src/routes/AdminRoutes.tsx:35-62: dashboard, requests, work-orders, conversations, notifications, slides, testimonials, reviews, partners, services, packages (three aliases: /cleaning-packages, /packages, /containers), container-system plus customer/employee/container profiles and contract print, ads, analytics, blog, SEO pages, settings, SEO panel, employees, profile, database and WhatsApp. Request print is separately mounted in src/App.tsx:37-38.
Admin page implementations correspond to src/pages/admin/{Dashboard,Requests,WorkOrders,Conversations,Notifications,Slides,Testimonials,Reviews,Partners,Services,Packages,ContainerSystem,ContainerRecordProfile,Analytics,Ads,Blog,SeoPages,SiteSettings,SEOPanel,Employees,AdminProfile,DatabaseManager,WhatsApp}.tsx plus Login.tsx and RequestPrint.tsx.
Major windows/forms
Main customer request flow is a global modal: src/components/home/ServiceRequestModal.tsx:103-215,268-523 — 3-step service -> container size -> details, then success; location text + GPS/map picker, immediate vs scheduled appointment, duration, customer/company, Saudi phone, email, notes. It posts /api/service-requests; preselected links can skip to details (:125-147).
Track order modal: src/components/home/TrackOrderModal.tsx:107-150,157-229 — number lookup, status/progress, cancellation/error states; opened from navbar/event dispatch (src/components/layout/Navbar.tsx:20-43,81-90,131-134).
Live support page: src/pages/Chat.tsx:181-261,263-358 — identity + optional package form, persistent localStorage conversation, text, image/camera attachment, location sharing. AI chatbot also embeds package-details form and can dispatch tracking (src/components/chat/AIChatbotWidget.tsx:390-435,614-614,1243).
Admin login: src/pages/admin/Login.tsx:18-45,65-104; role redirects drivers to work-orders and others to dashboard (:22-33).
Requests: create/edit RequestFormModal, detail RequestDetailModal, delete confirmation (src/pages/admin/Requests.tsx:120,570-608; modal implementations src/components/admin/RequestFormModal.tsx:86-176 and RequestDetailModal.tsx:129-177).
Work orders: order-detail dialog plus completion-evidence dialog/form (src/pages/admin/WorkOrders.tsx:154-526).
Container operations are the densest workflow: record dialog, contract wizard, assignment wizard (src/pages/admin/ContainerSystem.tsx:593-645,839-841; ContractWizard.tsx:58-127; ContainerAssignmentWizard.tsx:19-36). Includes contextual profile and printable contract routes.
Other CRUD/settings forms: reviews edit modal (src/pages/admin/Reviews.tsx:394-), profile/password (AdminProfile.tsx:163-195), database destructive confirmation (DatabaseManager.tsx:120-), conversations message composer (Conversations.tsx:573-), service/package/blog/slide/partner/testimonial/ads/settings/SEO editors in their respective admin files.
Repeated navigation patterns
Public navbar is fixed, RTL, same seven links on desktop/mobile: home, hash-linked containers/services, pricing, blog, FAQ, contact (src/components/layout/Navbar.tsx:8-16,52-117). Mobile repeats them in a right drawer with backdrop, CTA, tracking and admin login (:123-194).
Public footer repeats home/about/hash sections/FAQ/pricing/areas/blog/contact, then container category/service links, contact/social/map/area links (src/components/layout/Footer.tsx:63-95,97-167). Most inner pages additionally hand-roll a breadcrumb-style “الرئيسية / …” header (e.g. src/pages/ServiceDetail.tsx:286-297, src/pages/PackagesPage.tsx:83-97).
Persistent bottom-right phone + WhatsApp buttons and bottom-left AI assistant exist on every non-admin page (src/components/layout/FloatingContactButtons.tsx:9-15,29-75).
Admin uses one dark fixed sidebar + sticky top bar; sections are الرئيسية/المحتوى/سيستم الحاويات/الإعدادات (src/pages/admin/AdminLayout.tsx:16-42). Permission filters and role-specific navigation are applied at :90-103; mobile repeats sidebar as drawer (:370-391). Badges poll every 3s (:242-280). Drivers are forcibly restricted to /admin/work-orders (:314-351).
Biggest Services confusion points
Product identity is ambiguous: the site is branded primarily as container rental, while service catalog, cleaning packages, cleaning-company copy, container operations and SEO pages coexist. Evidence: request modal defaults to “حاويات الأنقاض” (ServiceRequestModal.tsx:125-145), while routes expose /services, /cleaning-packages, /packages, /containers and multiple aliases (PublicRoutes.tsx:64-86).
“Services” and “containers” are not distinct destinations: /services renders Home, /containers and /packages both render PackagesPage, and /admin/containers renders the packages admin (PublicRoutes.tsx:64-86; AdminRoutes.tsx:45-48). Users can’t predict whether they are browsing cleaning services, rental products, or container categories.
Navigation mixes full routes and same-page hashes (/#containers, /#services) with route pages; on inner pages these hashes depend on returning to /, while footer and detail pages use both patterns (Navbar.tsx:8-16; Footer.tsx:69-94; ServiceDetail.tsx:286-297).
Multiple duplicate English/Arabic/trailing-slash/legacy URLs increase mental model and canonicalization burden (PublicRoutes.tsx:32-100).
Order tracking is exposed in navbar only when settings load and feature is enabled (Navbar.tsx:83-90), but request success and chatbot use separate entry points; tracker accepts numeric IDs only (TrackOrderModal.tsx:129-142) although platform messaging displays #CF-2084 style IDs.
Request modal has a large decision tree (service, size, appointment, date/time, location text vs map, duration, customer identity) and can silently skip steps based on preselection (ServiceRequestModal.tsx:108-147,303-450), making “what do I need to provide?” unclear.
There are three support/contact paradigms competing for attention: CTA request modal, phone/WhatsApp floaters, AI widget, and /chat; chat also asks for identity/package before messaging (FloatingContactButtons.tsx:29-75; Chat.tsx:181-261).
Admin vocabulary overlaps: “الطلبات”, “مهام السائقين”, “عمليات الحاويات”, “الخدمات”, “كتالوج الباقات”, plus three package/container aliases. Permission-hidden navigation can make features appear missing (AdminLayout.tsx:90-103).
CleanFlow Platform (artifacts/sabaik-platform)
Route/page map
There is exactly one real route: router base /cleanflow-platform, / -> HomePage; every other URL -> local NotFound (src/App.tsx:566-580). Preview /cleanflow-platform/ matches this base.
Home is a single long-form marketing page composed in order at src/App.tsx:541-558: Hero, PlatformSection, WorkflowSection, CapabilitiesSection, IndustrySection, InsightsSection, TrackingSection, FAQSection, Footer.
Anchors are #top, #platform, #workflow, #capabilities, #insights, #tracking, #faq (src/App.tsx:288-313,325-342,392-420,461-501,516-537).
Major windows/forms
The only real data-entry window is a custom demo modal (src/App.tsx:240-275): full name, mobile, team count, current order-management method; submit is entirely local (setSent(true) at :241-245) and does not call API/email.
Interactive mock dashboard is visual only; “طلب جديد” opens the same demo modal (:164-237).
Insights “تصفية” button only toggles its label/state (:461-477); tracking card only hides/shows a hard-coded result (:483-499); FAQ is accordion (:505-525).
Repeated navigation patterns
Desktop/mobile top nav repeats five anchor links (#platform, #workflow, #capabilities, #insights, #faq) plus phone and demo CTA (src/App.tsx:91-155).
Hero, section cards, dashboard, FAQ and footer all repeat the demo CTA (App.tsx:197,298,376,454,518,535). Footer repeats four anchor links (:532-537).
Platform has no link to Services, admin login, actual product workspace, privacy/terms, or real contact flow; the logo only anchors to #top (:47-57).
Biggest Platform confusion points
It looks like an operational product (interactive dashboard, order statuses, analytics, tracking) but is a static marketing mock; dashboard/sidebar rows are non-interactive <div>s (App.tsx:164-237), demo submission never leaves the browser (:240-245), and tracking uses hard-coded CF-2084 data (:483-499).
“اطلب عرضاً عملياً”, “طلب جديد”, “شاهد المنصة على نشاطك” all open the same generic lead form, not a demo/account/onboarding flow (App.tsx:117-119,197,298,260).
“التصفية”, “عرض الكل”, “مصادر الطلبات”, analytics and workflow controls imply functionality but are decorative or state-only (App.tsx:215,227-230,461-477).
Platform and Services appear related but are technically separate apps: Platform has no API integration or cross-link to the Services app, while Services admin already contains many of the capabilities advertised. Users have no explicit choice between “book a cleaning/container service” and “buy/use the CleanFlow operating platform.”
The same term “tracking” means a real Services order tracker and a hard-coded Platform showcase card; this is especially easy to misread across products (Services/src/components/home/TrackOrderModal.tsx:129-150; Platform/src/App.tsx:483-499).
Anchor-only navigation provides no persistent route/page context, and “عرض الكل” points to #workflow rather than an order list (App.tsx:215).
Platform is Arabic RTL in content but uses an English-ish product/navigation vocabulary (Platform, ORDER → DONE, Web Push, KPI, SEO/LLMS) alongside Services’ Arabic operational labels, increasing cross-app terminology mismatch (App.tsx:294-296,404-405,346-355).

Subagent: Audit the container operations workflow across API and frontend: contract, request/work order, assignment, driver execution, evidence, completion, returns, finance, permissions. Identify existing capabilities, likely bugs, and missing safeguards. Read-only; return concise findings with exact file paths.
Task

Audit the container operations workflow across API and frontend: contract, request/work order, assignment, driver execution, evidence, completion, returns, finance, permissions. Identify existing capabilities, likely bugs, and missing safeguards. Read-only; return concise findings with exact file paths.
Show more

35 actions
Working
Result
Workflow audit (API + frontend)
Contract/request creation
Full workflow exists: contract + assignment + appointment + service request are created atomically, with customer/site/asset relationship checks, generated contract number, tax/total normalization, overlap checks, operation-key replay, and audit rows: artifacts/api-server/src/routes/containerSystem.ts:506-618; UI wizard drives the flow: artifacts/sabaik-almasa/src/pages/admin/ContractWizard.tsx:58-149, artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:665-722.
Likely bug: wizard always submits status: "active" and API workflow inserts active contract, so the approval lifecycle (draft/pending_approval -> approved) is bypassed; minimum-price exception is only checked on generic create, not the workflow route: artifacts/sabaik-almasa/src/pages/admin/ContractWizard.tsx:127-143; artifacts/api-server/src/routes/containerSystem.ts:538-550,571-580; generic minimum-price check only at :936-942.
Workflow appointment date/time is stored, but no validation that appointment is in the future, within contract dates, or compatible with appointmentType: artifacts/api-server/src/routes/containerSystem.ts:589-608.
requestId linking auto-creates a customer by name/phone and updates the request, but does not create/validate a customer site when absent; name matching can merge distinct customers: artifacts/api-server/src/routes/containerSystem.ts:348-407.
Assignment / dispatch
Assignment validates active driver, available vehicle, same-day driver/vehicle conflict, and resets driver evidence/status on reassignment: artifacts/api-server/src/routes/serviceRequests.ts:465-555; manager UI is implemented in artifacts/sabaik-almasa/src/pages/admin/WorkOrders.tsx:1097-1125.
Missing safeguard: assignment does not validate that request customer/container/contract IDs are mutually consistent, that the request is container work, or that the selected vehicle is compatible with the container/job. It also permits assigning a vehicle without a driver and does not reserve/lock the vehicle transactionally: artifacts/api-server/src/routes/serviceRequests.ts:467-543.
Conflict check is same calendar day, not overlapping time windows; two jobs at different times on the same day are incorrectly rejected, while malformed/null schedules avoid conflict checking: artifacts/api-server/src/routes/serviceRequests.ts:36-42,511-528.
Contract workflow creates an unassigned pending request; no automatic driver assignment: artifacts/api-server/src/routes/containerSystem.ts:596-608.
Driver execution / state machine
Driver-only list is correctly scoped by assignedDriverId; transitions are constrained assigned -> accepted/rejected -> started -> en_route -> arrived -> completed, and closed jobs cannot change: artifacts/api-server/src/routes/serviceRequests.ts:557-572,594-633; UI exposes corresponding controls: artifacts/sabaik-almasa/src/pages/admin/WorkOrders.tsx:630-699.
Completion is idempotency/audit-aware and atomically syncs contract, asset, movement, and request: artifacts/api-server/src/routes/serviceRequests.ts:641-728.
Major consistency hole: completion does not verify request containerRecordId matches the contract’s containerCode asset, does not verify contract/assignment ownership or expected contract state, and does not re-check asset transition legality before updating. A second work order for the same contract can therefore overwrite lifecycle/movement state: artifacts/api-server/src/routes/serviceRequests.ts:44-61,66-140.
Return jobs created by the wizard start with the asset reserved, but return transition only permits rented/with_customer/awaiting_return/in_transit; pickup/return workflows can fail at completion: artifacts/api-server/src/routes/containerSystem.ts:163-170; wizard appointment type is pickup but asset is always reserved: artifacts/sabaik-almasa/src/pages/admin/ContractWizard.tsx:200-202; workflow always writes assignment reserved: artifacts/api-server/src/routes/containerSystem.ts:581-588.
Driver may complete non-container work without evidence. For container work, API requires receiver name, signature and photo, but GPS remains optional and can be manually fabricated: artifacts/api-server/src/routes/serviceRequests.ts:650-653; UI only requires name/photo/signature and offers optional manual GPS: artifacts/sabaik-almasa/src/pages/admin/WorkOrders.tsx:440-489,511-515.
Evidence has no size/content/ownership binding beyond image MIME supplied by client; signature is unbounded base64 and not cryptographically/verifiably tied to receiver/job/time/location. Uploaded evidence is served publicly via static /api/uploads: artifacts/api-server/src/routes/uploads.ts:24-32,42-64; artifacts/api-server/src/app.ts:87-91.
driverName in movement payload is incorrectly populated with numeric assignedDriverId: artifacts/api-server/src/routes/serviceRequests.ts:100-114.
Evidence / privacy
Public GET /service-requests/:id returns the complete request, including phone, notes, driver GPS, signature data and proof URL, without authentication; this also makes evidence URLs discoverable if IDs are known: artifacts/api-server/src/routes/serviceRequests.ts:417-425; schema fields are all persisted directly: lib/db/src/schema/serviceRequests.ts:21-36.
Upload route authentication is stronger for driver uploads (requireAdmin + requireDriver), but public generic /api/uploads remains available and all stored files are static/public: artifacts/api-server/src/routes/uploads.ts:49-64; artifacts/api-server/src/app.ts:87-91.
Completion / returns / movements
Atomic admin lifecycle supports approve/reject/deliver/return with movement and audit: artifacts/api-server/src/routes/containerSystem.ts:766-913.
Frontend exposes settle, debt, and close, but only deliver/return/approve/reject call the lifecycle API. The other actions directly patch arbitrary status/payload and do not create financial entries, enforce state transitions, or require role-specific approval: artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:761-803; generic patch is permissive at artifacts/api-server/src/routes/containerSystem.ts:1129-1200.
Admin lifecycle deliver/return itself does not require evidence, receiver, signature, assigned driver, or work-order linkage; staff can mark delivery/return directly, bypassing field controls: artifacts/api-server/src/routes/containerSystem.ts:823-877.
Generic movement creation first inserts the movement then syncs asset; on sync failure it archives the movement in a separate operation, and no transaction covers insert + asset update + audit. Concurrent movements can pass stale status checks: artifacts/api-server/src/routes/containerSystem.ts:1010-1021,1038-1099.
Movement edit is rejected, which is good, but movement evidence is only syntactically validated (optional GPS, no required photo/signature/actor/job): artifacts/api-server/src/routes/containerSystem.ts:174-183,1135-1169.
Finance
Capabilities exist for invoice/contract totals, payment/receipt validation, invoice-return cap, contract ledgers, settlement idempotency and payment ledger rows: artifacts/api-server/src/routes/containerSystem.ts:74-95,249-289,620-674,676-754; UI settlement workspace: artifacts/sabaik-almasa/src/pages/admin/ContainerSystemSpecialPages.tsx:94-161.
Critical bug: generic payment/receipt creation validates positive amount and linked contract/invoice but does not cap payment against remaining contract balance; only /financial/settle enforces overpayment: artifacts/api-server/src/routes/containerSystem.ts:249-267,1028-1031; settlement cap at :705-708.
Settlement idempotency is application-level table scanning, with no database unique constraint/transactional precondition; concurrent requests can double-post. Generic financial record creation inserts its ledger entry after the main transaction, so a failure can leave an unbalanced financial record: artifacts/api-server/src/routes/containerSystem.ts:38-45,690-700,1109-1125; schema has no uniqueness/foreign keys: lib/db/src/schema/containerSystem.ts:5-24.
Deposits can be linked repeatedly/overwritten (linkedPaymentId), and ledger calculations aggregate records by loose contract/invoice strings rather than immutable IDs: artifacts/api-server/src/routes/containerSystem.ts:641-653,741-747.
Permissions / authorization
Global /api/admin auth blocks drivers, and driver routes are scoped correctly: artifacts/api-server/src/app.ts:93-95; artifacts/api-server/src/middleware/adminAuth.ts:43-90.
However, all authenticated non-driver roles can read container-system snapshot, records and audit because those routes have no permission middleware; UI-only sidebar permissions are not enforcement. artifacts/api-server/src/routes/containerSystem.ts:410-504,1222-1225; role defaults/custom permissions: lib/db/src/schema/admins.ts:8-54.
PATCH /service-requests/:id blocks drivers but otherwise lets any authenticated admin role alter status and relationship IDs without validation or workflow audit: artifacts/api-server/src/routes/serviceRequests.ts:427-463.
Severe authorization flaw: DELETE /service-requests/:id uses only requireAdmin; a driver token is accepted because the route is /api/service-requests, not /api/admin, and it can delete any request: artifacts/api-server/src/routes/serviceRequests.ts:731-738; artifacts/api-server/src/middleware/adminAuth.ts:17-40.
Employee update accepts arbitrary role strings and arbitrary permission strings without allowlist/privilege-boundary validation; a manager can assign powerful custom permissions to non-admin users: artifacts/api-server/src/routes/employees.ts:82-120.
Tokens have no expiry check: generateToken stores ts, but verifyToken only verifies signature and parses payload: artifacts/api-server/src/routes/auth.ts:30-47.
Missing safeguards / priority fixes
Enforce server-side role/section permissions on every container read/write/audit route; fix request delete authorization immediately.
Make contract/assignment/work-order relationships immutable and validate them in every mutation; add DB foreign keys/unique indexes for operation keys, document numbers, active assignment, and one active dispatch per contract.
Centralize lifecycle transitions; remove generic status patch for contracts/financial records or require explicit, role-gated commands.
Require evidence policy (and preferably GPS/time/device checks) for all field completions, bind uploads privately to job ID, and redact public tracking responses.
Make movement + asset + contract + work-order completion one transaction with current-state guards; correct return initialization and use immutable IDs for finance reconciliation.
Add token expiry/rotation and validate employee roles/permissions against an allowlist and caller’s privilege ceiling.

Subagent: Audit the frontend UI for usability and information architecture. Focus on admin/platform screens, forms, dialogs, duplicated features, unclear labels, responsive/mobile behavior, loading/error/empty states. Read-only; return prioritized recommendations with exact file paths.
Task
Audit the frontend UI for usability and information architecture. Focus on admin/platform screens, forms, dialogs, duplicated features, unclear labels, responsive/mobile behavior, loading/error/empty states. Read-only; return prioritized recommendations with exact file paths.

45 actions
1 failed
Working
Result
Prioritized frontend UI/IA audit
P0 — trust, correctness, and destructive-action risks
Platform marketing form does not submit anywhere. artifacts/sabaik-platform/src/App.tsx:240-270 marks the demo request as sent entirely in local state; no API call, validation feedback, pending state, or network error exists. Users are told “وصل طلبك إلى فريقنا” despite no lead being created. Wire it to a real endpoint, disable during submission, show field-level/server errors, and preserve/retry input.
Platform order tracking is a non-functional demo. artifacts/sabaik-platform/src/App.tsx:483-499 uses a hard-coded CF-2084 and only toggles visibility; changing the order number never queries or validates anything. Label this explicitly as a preview, or implement lookup with loading/not-found/error states.
Database manager exposes destructive delete controls without a robust safety model. artifacts/sabaik-almasa/src/pages/admin/DatabaseManager.tsx:120-163,210-230,320-327 allows row deletion from arbitrary tables, with a custom dialog but no typed confirmation, undo, audit explanation, or in-progress button state. Add table/row impact context, permission gating, disabled state while deleting, and ideally restrict deletion to supported business records.
Several custom dialogs lack semantic dialog/accessibility behavior. Employee add/edit and delete overlays are plain fixed divs (artifacts/sabaik-almasa/src/pages/admin/Employees.tsx:288-304,465-492) with no role="dialog", aria-modal, labelled close buttons, focus trap, Escape handling, or focus return. The close control is icon-only without aria-label (:301-303). Use the shared Dialog/AlertDialog components consistently.
Nested interactive buttons in permission groups are invalid and confusing. artifacts/sabaik-almasa/src/pages/admin/Employees.tsx:167-184 nests the “toggle group” button inside the group expand button. This creates ambiguous click/keyboard behavior and invalid interactive nesting. Make the checkbox/toggle and disclosure separate siblings, with explicit aria-expanded and accessible labels.
P1 — primary workflow usability and information architecture
Admin navigation does not expose all routed/content features. Routes exist for slides, testimonials, partners (artifacts/sabaik-almasa/src/routes/AdminRoutes.tsx:10-18,40-43), but ALL_NAV omits them (artifacts/sabaik-almasa/src/pages/admin/AdminLayout.tsx:16-35). They are only discoverable as tabs inside Site Settings (artifacts/sabaik-almasa/src/pages/admin/SiteSettings.tsx:1616-1662). Conversely, Employees permission inventory includes slides, testimonials, and partners (artifacts/sabaik-almasa/src/pages/admin/Employees.tsx:20-38) while the global sidebar cannot navigate them. Choose one canonical IA: either “Website content” with these as nav destinations, or remove duplicate routes and clearly group all content under Settings.
The admin has duplicated/overlapping feature surfaces and terminology. Requests supports direct assignment and status changes (artifacts/sabaik-almasa/src/pages/admin/Requests.tsx:456-551), while Work Orders separately supports assignment and lifecycle actions (artifacts/sabaik-almasa/src/pages/admin/WorkOrders.tsx:621-700,1097-1120). Container System adds another “أوامر العمل الميدانية” link (artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:78-82). Define a single source of truth: request intake vs dispatch board, with clear handoff and status ownership; avoid presenting three places to do the same assignment.
Container System is an extremely large, flat back-office IA. Its sidebar contains roughly 50 destinations across operations, contracts, assets, finance, HR, inventory, reports, and settings (artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:44-134). This is too much cognitive load and mixes entities, tasks, reports, and configuration. Promote a small set of role-based hubs (Today/Operations, Customers & contracts, Fleet, Finance, Reports, Settings), move low-frequency records behind “More,” and add breadcrumbs/contextual back links.
Some Container System navigation/report items appear to be labels rather than actionable features. container_assignment is declared as a nav key but has no viewKind mapping (artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:136-149), and report cards render as non-clickable cards (artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:462-467). Ensure every visible destination has a real action/route, or style it as unavailable with explanation.
Requests table is desktop-first and functionally overloaded on mobile. It has eight columns plus a min-width driver selector and four action/status controls in one row (artifacts/sabaik-almasa/src/pages/admin/Requests.tsx:414-565). Horizontal scrolling hides critical information and makes assignment/status changes hard to use. Use responsive request cards or a compact primary table with an actions menu/details drawer; keep status and assignment as prominent, separate controls.
Database manager’s two-pane layout is not mobile-adapted. It always renders a w-56 table pane beside the viewer (artifacts/sabaik-almasa/src/pages/admin/DatabaseManager.tsx:442-468), while table content itself uses min-w-max (:275-277). On phones this becomes a cramped split plus nested horizontal scroll. Switch to a list-first mobile view and navigate to the table viewer, with a clear Back action.
Container System record rows intentionally hide most information on mobile. Fields and status are hidden below sm (artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:257-270), leaving only a primary value and icon actions. Convert each row to a stacked card showing status/date and the most important identifying field; do not require opening every record to know operational state.
P1 — forms and feedback
Request create/edit form has weak validation and silent mutation failures. artifacts/sabaik-almasa/src/components/admin/RequestFormModal.tsx:141-165,354-361 sends mutations with no onError, no visible field errors, and generic required rules. A failed save leaves the dialog open without explaining why; phone/date formats are not validated. Add mutation pending text, server error banner, field-level messages, phone validation, and require scheduledAt when “موعد محدد” is selected.
Request list has no explicit loading or error state. useGetServiceRequests is destructured without isLoading/isError/error (artifacts/sabaik-almasa/src/pages/admin/Requests.tsx:125-140), and empty rows render “لا توجد طلبات لعرضها” (:557-563) even during failure or before the first response. Add skeletons, retry, and distinguish “no records” from “filter has no matches.” Status update also only refetches on success and has no onError (:188-192).
Dashboard converts API failure/missing data into a plausible zero dashboard. artifacts/sabaik-almasa/src/pages/admin/Dashboard.tsx:293-320 only handles isLoading; it does not handle query error and falls back to DUMMY, showing zeros and “لا توجد بيانات” as if the business has no activity. Add an error state with retry and retain stale data with an “updated/refresh failed” banner.
Conversations lacks loading/error states and can look empty during failure. artifacts/sabaik-almasa/src/pages/admin/Conversations.tsx:129-145,354-407 uses query data directly and only renders “لا توجد محادثات نشطة” for an empty list; there is no initial spinner/skeleton, error banner, retry, or message-loading state. Add distinction between loading, failed, empty, and closed-only results. Also make delete actions accessible on touch: they are hover-only (:391-401) and therefore effectively undiscoverable on mobile.
WhatsApp settings treats failed reads as a usable blank form and ignores HTTP failure on save. artifacts/sabaik-almasa/src/pages/admin/WhatsApp.tsx:108-146 catches settings load without recording an error, and saveSettings never checks res.ok before showing success (:128-146). Add a persistent error/retry state, response validation, and a clear unsaved-changes model. Message loading similarly clears/retains silently on failure (:117-123).
Reviews uses browser confirm() and silently drops API failures. artifacts/sabaik-almasa/src/pages/admin/Reviews.tsx:45-64,71-103 has no auth headers, no visible load error, and no toast/error for failed approve/reject/delete operations; deletion relies on a browser confirm. Use shared AlertDialog, actionable errors, and explicit pending labels per row.
Many content CRUD pages have no query error/loading contract. Slides directly defaults query data to [] (artifacts/sabaik-almasa/src/pages/admin/Slides.tsx:27-31) and save/delete callbacks only handle success (:53-70); Services uses the same pattern (artifacts/sabaik-almasa/src/pages/admin/Services.tsx:367-374). This risks presenting an empty catalog when the API fails and gives no reason when actions fail. Standardize a CRUD state component across Slides, Services, Packages, Blog, Partners, and Testimonials.
Employee search is not locale-normalized and has no result count/clear affordance. artifacts/sabaik-almasa/src/pages/admin/Employees.tsx:541-585 uses case-sensitive Arabic/Latin includes, and the only empty state is generic. Normalize Arabic/Latin search, expose “X of Y,” provide clear search, and preserve filters while refreshing.
P2 — labels, clarity, and accessibility
Terminology is inconsistent between navigation, permissions, and pages. Examples: “لوحة القيادة” vs “الرئيسية,” “سيستم الحاويات” vs “نظام الحاويات الكامل,” “طلبات” vs “أوامر عمل,” and “كتالوج الباقات” vs “حاويات الأنقاض والنفايات” (artifacts/sabaik-almasa/src/pages/admin/AdminLayout.tsx:17-41; artifacts/sabaik-almasa/src/pages/admin/Employees.tsx:20-77; artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:44-134). Establish a shared Arabic label dictionary and use task language consistently.
Several icon-only controls lack accessible names. Examples include mobile drawer close (artifacts/sabaik-almasa/src/pages/admin/AdminLayout.tsx:375-380), employee edit/delete (artifacts/sabaik-almasa/src/pages/admin/Employees.tsx:667-680), Slides edit/delete/toggle (artifacts/sabaik-almasa/src/pages/admin/Slides.tsx:199-224), and database refresh/back/delete controls (artifacts/sabaik-almasa/src/pages/admin/DatabaseManager.tsx:239-259,320-327). Add aria-label, visible tooltips where appropriate, and focus-visible styles.
Admin badges are visually emphasized but not semantically explained. Sidebar counts show only numbers (artifacts/sabaik-almasa/src/pages/admin/AdminLayout.tsx:124-135); “conversationCount” and “pendingRequests” do not convey whether they are unread, open, or pending. Use labels/tooltips and aria-label such as “5 طلبات معلقة.”
Request detail duplicates the conversation action three times and uses animated/pulsing urgency everywhere. artifacts/sabaik-almasa/src/components/admin/RequestDetailModal.tsx:236-281 repeats “محادثة” in the online banner and phone row, while the request list also has a pulsing “محادثة 🟢” action (artifacts/sabaik-almasa/src/pages/admin/Requests.tsx:512-520). Keep one primary CTA and one status indicator; reserve animation for new/unread state to reduce distraction.
Labels in request form do not match business meaning. “حجم ومقاس الحاوية” (artifacts/sabaik-almasa/src/components/admin/RequestFormModal.tsx:232-234) combines redundant terms; “مدة الاستئجار” is also used for non-container service contexts (:296-303). Use conditional labels (e.g., “حجم الحاوية,” “مدة/تكرار الخدمة”) and explain required vs optional fields.
Platform visual dashboard uses tiny text and decorative data without clear product boundaries. artifacts/sabaik-platform/src/App.tsx:164-235 and artifacts/sabaik-platform/src/index.css:129-139 render dashboard labels at 7–10px and hard-coded KPIs. On mobile the CSS hides the chart panel and sidebar (artifacts/sabaik-platform/src/index.css:167-177), which removes important context rather than reflowing it. Increase minimum text sizes, use real accessible text/table equivalents, and explicitly label the whole visual as a sample/demo.
Platform mobile header removes phone/contact access. At artifacts/sabaik-platform/src/index.css:162-169, .nav-actions (including the phone number) is hidden below 768px; the mobile menu only includes navigation and demo CTA (artifacts/sabaik-platform/src/App.tsx:130-153). Keep a tap-to-call action in the mobile menu or header.
P2 — states and operational polish
System status can show misleading bot state and has no loading/error indication. artifacts/sabaik-almasa/src/pages/admin/Dashboard.tsx:209-213,215-262 fetches settings and silently catches failures; the bot label is inferred from support status, so an API failure displays “غير متاح/يعمل” as a real state. Add loading skeletons, an error state, and a direct bot-status field.
Auth verification can redirect without explaining a transient network failure. artifacts/sabaik-almasa/src/pages/admin/AdminLayout.tsx:205-239 clears tokens and redirects to login on any .catch(), including temporary server/network errors. Distinguish invalid session (logout) from connectivity failure (retry screen) to avoid forcing users through login unnecessarily.
Form/image handling lacks failure recovery in several places. Slide preview simply renders an image with no error fallback (artifacts/sabaik-almasa/src/pages/admin/Slides.tsx:148-151); RequestForm hides broken images (artifacts/sabaik-almasa/src/components/admin/RequestFormModal.tsx:252-259). Show a labelled preview fallback and allow replacing/removing the bad URL.
Positive patterns worth standardizing
Work Orders has the strongest loading/error/empty treatment and retry action (artifacts/sabaik-almasa/src/pages/admin/WorkOrders.tsx:1022-1081); reuse this state model elsewhere.
Container System provides useful skeletons, explicit empty states, and mobile-aware record actions (artifacts/sabaik-almasa/src/pages/admin/ContainerSystem.tsx:219-289).
Request detail and work-order dialogs generally use scroll-constrained responsive sizing (artifacts/sabaik-almasa/src/components/admin/RequestDetailModal.tsx:175-179; artifacts/sabaik-almasa/src/pages/admin/WorkOrders.tsx:173-176), but this should be paired with the accessibility fixes above.

Worked for 1 minute
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.