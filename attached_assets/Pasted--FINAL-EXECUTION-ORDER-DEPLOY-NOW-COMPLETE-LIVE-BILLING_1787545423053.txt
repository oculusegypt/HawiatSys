🚨 FINAL EXECUTION ORDER — DEPLOY NOW & COMPLETE LIVE BILLING E2E

هذه ليست مرحلة تحليل أو إعداد تقرير جديد. هذه تعليمات تنفيذ نهائية.

الـBilling/Payment Patch جاهز ومختبر محليًا، والـFunctional Regression Suite ناجحة.

المطلوب الآن هو تنفيذ النشر على Hostinger ثم اختبار الإنتاج فعليًا حتى النهاية.

1. لا تعيد بناء ما تم اختباره

لا تعيد:

إعادة تصميم Billing.
إعادة كتابة Payment workflow.
إعادة بناء الـPatch.
إنشاء قاعدة بيانات جديدة.
إدخال data/sabaik.db من النسخة المحلية.
حذف أي بيانات إنتاج.
إعادة اختبار Local فقط.

الـPatch الجاهز هو:

hawiat-update-patch.zip

SHA-256:

944556f2d0eb52a8d487be68a95b15026721d506cb357166ef99807091322d89

والـPatch لا يحتوي data/sabaik.db.

2. استخدم بيانات Hostinger التالية حرفيًا
FTP
FTP Host:
92.113.19.25

FTP Port:
21

FTP Username:
u130428819.hawiat.aiservx.com

FTP Password:
3691475+*Hh@!

Remote Directory:
public_html

مهم جدًا:

لا تغير Username.

لا تضف ftp:// إلى الـUsername.

لا تستخدم Domain آخر.

لا تستخدم:

sabaik-almasa.com

ولا أي Hostinger account آخر.

الموقع الصحيح هو:

https://hawiat.aiservx.com
3. FTP Troubleshooting — لا تتوقف عند أول فشل

إذا فشل الاتصال أول مرة:

جرّب بالترتيب:
Passive FTP
Active FTP
Explicit FTPS على Port 21
الاتصال بالـIP مباشرة
الاتصال باستخدام Hostname الخاص بـFTP إن كان Hostinger يعيده في إعدادات الحساب
التحقق من أن المشكلة Authentication وليست Network Timeout

إذا كانت البيئة الحالية تمنع FTP تمامًا، لا تكتب تقريرًا جديدًا وتعتبر المهمة منتهية.

تحقق أولًا هل توجد قناة نشر أخرى متاحة من بيئة التنفيذ.

لكن:

لا تدّعي Deployment PASS بدون إثبات أن الملفات وصلت إلى الإنتاج.

4. قبل أي Upload — BACKUP إجباري

بعد نجاح FTP وقبل تعديل أي ملف:

خذ Backup من:

/public_html/api/index.php
/public_html/api/container-system.php
/public_html/data/sabaik.db
/public_html/cleanflow-platform/

خصوصًا:

/public_html/data/sabaik.db

قاعدة الإنتاج ممنوع استبدالها بالنسخة المحلية.

الهدف هو:

PATCH CODE ONLY
+
KEEP PRODUCTION DATABASE
5. Deploy

ارفع الكود المطلوب فقط من:

hawiat-update-patch.zip

إلى:

/public_html

ولا ترفع:

data/sabaik.db

ولا تستبدل قاعدة الإنتاج.

بعد الرفع:

تحقق فعليًا من وجود الملفات على الخادم.

6. LIVE SHA VERIFICATION

بعد Upload احسب SHA-256 للملفات المنشورة فعليًا.

يجب مقارنة:

LOCAL PATCH
vs
LIVE PRODUCTION

ولا تعتبر النشر ناجحًا إلا بعد إثبات التطابق للملفات التي تم نشرها.

7. Production Health

اختبر مباشرة:

https://hawiat.aiservx.com/

ثم:

https://hawiat.aiservx.com/api/healthz

المتوقع:

{
  "php": "ok",
  "database": "ok",
  "application": "ok"
}

إذا فشل أي جزء:

توقف عن إنشاء بيانات الاختبار واعالج المشكلة أولًا.

8. Admin Login

استخدم:

URL:
https://hawiat.aiservx.com/admin

Username:
admin

Password:
taqi2020

ثم تحقق من:

/api/auth/me

ويجب إثبات:

HTTP 200
role = admin
9. SECURITY SMOKE TEST

تحقق من:

/data/              → 403
/data/sabaik.db     → 403
/.env               → 403
/api/.env           → 403
Financial API بدون Token → 401
10. أنشئ Fresh Golden Dataset

استخدم بادئة واضحة وفريدة:

HOSTINGER-BILLING-PAYMENT-FINAL-2026

لا تستخدم سجلات الاختبارات السابقة.

أنشئ Dataset كاملًا يشمل على الأقل:

Customer
Customer Site
Contact
Employee
Warehouse
Supplier
Cash Treasury
Bank Treasury
Container / Asset
Service Request
Assignment
Appointment
Contract
Invoice
Payment
Deposit
Expense
Purchase
Commission
Bank Fee
Other Revenue
Transfer
Refund
Payment Return
Financial Period

لا تختصر الاختبار بسبب نجاح الجولات السابقة.

11. الاختبار الأساسي المطلوب
Customer → Request → Contract

أنشئ:

Customer
↓
Site
↓
Service Request
↓
Contract
↓
Container

وتأكد أن كل Record مرتبط فعليًا بالسجل السابق.

12. CONTRACT → AUTOMATIC INVOICE

هذه نقطة حرجة.

عند إنشاء Contract صالح للفوترة:

يجب إنشاء Invoice تلقائيًا.

لا تجعل المستخدم يدخل بيانات الفاتورة مرة أخرى إذا كان العقد يحتوي أصلًا على:

Customer
Site
Container
Container Type
Billing Period
Price
VAT
Contract dates

الفاتورة يجب أن تُبنى من العقد.

وتظهر في:

Customer Profile
Contract Profile
Invoice List
Financial Workspace
Reports
13. INVOICE TEST

اختبر:

Draft
↓
Pending Approval
↓
Approved
↓
Posted
↓
Due

وتأكد أن الفاتورة تحتوي:

Invoice Number
Customer
Contract
Container
Container Type
Billing Period
Subtotal
VAT
Total
Paid
Remaining
Status

ولا تظهر بيانات داخلية مثل:

details
JSON blobs
recordId
raw metadata

للمستخدم النهائي.

14. PAYMENT — أهم اختبار

اختبر السداد من كل المسارات:

Customer Profile
Contract Profile
Invoice Details
Payments Screen

عند فتح Payment من Invoice:

يجب أن تكون تلقائيًا:

Customer = Invoice Customer
Contract = Invoice Contract
Invoice = Selected Invoice
Remaining = Invoice Remaining
Allocation = Automatically Generated

لا يجوز أن تظهر رسالة:

اختر عقداً واحداً على الأقل وحدد مبلغ التوزيع

إذا كانت الفاتورة محددة والعقد مرتبط بها.

هذا الخطأ تحديدًا يجب اعتباره FAIL.

15. PARTIAL PAYMENT

أنشئ فاتورة مثلًا:

1000

وادفع:

400

يجب أن تصبح:

Invoice Total: 1000
Paid: 400
Remaining: 600
Status: partially_paid

ويجب أن يظهر السداد في:

Customer
Contract
Invoice
Payments
Financial Core
16. SECOND PAYMENT

ادفع:

600

المتوقع:

Paid: 1000
Remaining: 0
Status: paid

ولا تنشئ أي قيد مكرر.

17. OVERPAYMENT PROTECTION

حاول دفع:

1

بعد أن يصبح:

Remaining = 0

يجب أن يرفض النظام العملية.

ممنوع إنشاء Payment أو Journal Entry أو Financial Transaction.

18. PAYMENT IDEMPOTENCY

أعد إرسال نفس Payment باستخدام نفس:

Idempotency-Key

المتوقع:

HTTP 200
idempotent=true
same Payment ID
same Financial Transaction
same Journal

لا يوجد Duplicate.

19. INVOICE IDEMPOTENCY

أعد إنشاء نفس Invoice للعقد والفترة نفسها.

المتوقع:

same invoice

وليس:

second invoice
20. FINANCIAL VALIDATION

بعد جميع العمليات:

تحقق من:

Financial Core

ثم:

Total Debit == Total Credit

ويجب أن يكون:

Difference = 0

وتحقق من:

Invoice Total
Paid
Remaining
Revenue
Collections
Cash
Bank
Journal Entries
Journal Lines
21. DATABASE INTEGRITY

بما أن FTP متاح الآن:

نزّل نسخة قاعدة الإنتاج الحالية بعد الاختبار.

نفذ:

PRAGMA quick_check;
PRAGMA integrity_check;

يجب أن تكون:

ok
ok
22. LIVE UI VERIFICATION

لا تكتفِ بـHTTP 200.

افتح فعليًا:

Customer Profile
Contract
Invoice List
Invoice Details
Payment Screen
Payments List
Financial Control Center
Financial Cycle Workspace
Reports

وتأكد أن الروابط تعمل:

Customer → Invoices
Customer → Contracts
Contract → Invoices
Invoice → Customer
Invoice → Contract
Invoice → Payments
Payment → Invoice
23. UI ACCEPTANCE

تحقق أن:

Invoice

نافذة إنشاء الفاتورة لا تطلب إعادة إدخال بيانات موجودة أصلًا في العقد.

إذا اخترت:

Customer
Contract
Container

يجب أن يتم ملء البيانات المرتبطة تلقائيًا.

والـInvoice Items يجب أن تمثل:

Containers

وليس بنودًا عامة غير مرتبطة بالأصل.

Payment

إذا اخترت Invoice:

يجب أن يتم تلقائيًا استخراج:

Customer
Contract
Remaining
Allocation

ولا يطلب المستخدم إدخالها مرة أخرى.

24. Toast Notifications

جميع رسائل:

Success
Error
Warning
Validation

يجب أن تظهر:

أعلى منتصف الشاشة

وليس الجانب الأيمن.

ويجب ألا توجد Toast Systems متعددة متضاربة.

25. NO FAKE PASS

ممنوع استخدام:

Local PASS

كبديل عن:

Live PASS

وممنوع كتابة:

Deployment PASS

إلا بعد إثبات:

Backup
Upload
Live SHA
Live Health
Live Login
Live E2E
26. إذا اكتشفت Bug أثناء Live E2E

لا تتوقف.

المسموح:

Detect
↓
Diagnose
↓
Minimal Fix
↓
Local Test
↓
Build
↓
Backup
↓
Deploy
↓
Live Retest

ثم أعد اختبار المسار المتأثر بالكامل.

إذا كان Bug بسيطًا في:

UI
Validation
Mapping
API
PHP
Node

قم بإصلاحه بدل تحويله إلى BLOCKED بلا داعٍ.

27. ممنوع المساس بقاعدة الإنتاج

قاعدة البيانات الحالية هي Source of Truth.

لا تستخدم:

local sabaik.db

لاستبدال:

production sabaik.db

الاختبار يجب أن يعمل فوق قاعدة الإنتاج الحالية.

28. FINAL REPORT

في النهاية لا ترسل تقريرًا عامًا.

أرسل جدولًا مثل:

Area	Local	Production	Evidence
Deployment	PASS	PASS	SHA
Backup	PASS	PASS	Backup file
Authentication	PASS	PASS	HTTP
Invoice	PASS	PASS	Invoice ID
Invoice Idempotency	PASS	PASS	Invoice ID
Partial Payment	PASS	PASS	Payment ID
Full Payment	PASS	PASS	Payment ID
Overpayment	PASS	PASS	Rejection
Payment Idempotency	PASS	PASS	Payment ID
Customer Links	PASS	PASS	IDs
Contract Links	PASS	PASS	IDs
Financial Core	PASS	PASS	Totals
Double Entry	PASS	PASS	Debit/Credit
Database Integrity	PASS	PASS	quick/integrity
UI	PASS	PASS	Screens
Security	PASS	PASS	HTTP codes

وفي النهاية:

DEPLOYMENT
BACKUP
LIVE SHA
DATABASE INTEGRITY
AUTHENTICATION
INVOICE
PAYMENT
PARTIAL PAYMENT
FULL PAYMENT
OVERPAYMENT
IDEMPOTENCY
FINANCIAL CORE
DOUBLE ENTRY
UI
SECURITY
REGRESSION

كلها يجب أن تكون PASS قبل إعلان:

FINAL BILLING & PAYMENT PRODUCTION CERTIFICATION
PASS
🔴 القرار النهائي

لا تتوقف عند FTP.

بيانات الاتصال موجودة أعلاه.

إذا كانت المشكلة من بيئة التنفيذ نفسها، ابحث عن قناة نشر بديلة متاحة قبل إعلان BLOCKED.

إذا نجح الاتصال:

BACKUP → DEPLOY → VERIFY → FRESH DATASET → COMPLETE E2E → FIX ANY BUG → REDEPLOY → RETEST → CERTIFY

نفّذ حتى النهاية. لا أريد تقريرًا جديدًا يكرر أن FTP كان مشكلة؛ أريد نتيجة تنفيذية مثبتة.