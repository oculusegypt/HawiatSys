ابعت له الرسالة دي كما هي، وخلّيه **ما يوقفش عند حد الـAPI أو يقول Partial**. المطلوب الآن إغلاق الشهادة فعليًا، وليس كتابة تقرير جديد:

---

# 🔴 FINAL LIVE E2E — EXECUTE TO COMPLETION — NO PARTIAL CERTIFICATION

أنت الآن في **المرحلة النهائية** من المشروع، والنسخة المنشورة على Hostinger تم إثبات سلامة Deployment وSHA وقاعدة البيانات وAuthentication وSecurity.

**ممنوع اعتبار المهمة منتهية بتقرير PARTIAL جديد.**

المطلوب الآن هو تنفيذ **دورة Live E2E كاملة فعلية على الإنتاج** باستخدام النسخة المنشورة، ثم إصلاح أي خلل يظهر وإعادة الاختبار حتى PASS.

## القاعدة الأساسية

لا أريد:

* `PARTIAL`
* `NOT TESTED`
* `NOT APPLICABLE`
* `لم أتمكن`
* `سأضيف مهمة لاحقة`
* تقرير جديد بدون تنفيذ
* نجاح شكلي مبني على قراءة بيانات موجودة مسبقًا

أريد:

> **CREATE → CONTRACT → WORK ORDER → DRIVER → DELIVERY → INVOICE → PARTIAL PAYMENT → FULL PAYMENT → IDEMPOTENCY → PICKUP → EMPTYING → FINAL VERIFICATION**

وكل ذلك على **Dataset جديد ومعزول**.

---

# 1. LIVE DATASET

أنشئ Dataset جديدًا فريدًا بهذه البادئة:

```text
FINAL-LIVE-E2E-2026
```

ويجب أن تكون كل السجلات الجديدة قابلة للتتبع بهذه البادئة.

أنشئ على الإنتاج:

* Customer
* Customer Site
* Contact إن كان مطلوبًا
* Employee/Driver
* Warehouse إن كان مطلوبًا
* Container/Asset
* Contract
* Work Orders
* Invoice
* Payments

**لا تستخدم سجلات الاختبارات القديمة كبديل عن إنشاء Dataset جديد.**

ولا تحذف أو تعدل بيانات الإنتاج القديمة.

---

# 2. CUSTOMER → CONTRACT

أنشئ عميلًا جديدًا ثم موقعه ثم حاوية جديدة ثم عقدًا جديدًا.

تحقق من:

```text
Customer created
Site created
Container created
Contract created
```

ثم أعد إنشاء العقد بنفس `Idempotency-Key`.

يجب أن تكون النتيجة:

```text
same contract
same work order
no duplicate
```

وتحقق من عدد السجلات قبل وبعد.

---

# 3. CONTRACT → WORK ORDER

هذه نقطة حرجة جدًا.

**العمليات التشغيلية ليست Customer Requests.**

يجب أن تستخدم:

```text
work_order
```

وليس:

```text
service_request
```

للعمليات:

* Delivery
* Pickup / Return
* Emptying
* أي عملية تشغيل ميداني مرتبطة بالعقد

تحقق صراحةً:

```text
service_requests before
service_requests after
difference = 0
```

للعمليات التشغيلية الجديدة.

وفي المقابل:

```text
work_orders increased correctly
appointments increased correctly
```

---

# 4. DRIVER ASSIGNMENT

أنشئ/استخدم Driver اختبار حقيقي.

قم بإسناد أمر التشغيل إليه.

ثم اختبر دورة السائق كاملة:

```text
accepted
started
en_route
arrived
completed
```

لا تعتبر الأمر ناجحًا بمجرد تغيير الحالة.

تحقق من:

* driver assignment
* work order state
* appointment
* container state
* contract state
* audit trail

---

# 5. DELIVERY

نفذ التسليم فعليًا.

تحقق من:

```text
Work Order = completed
Contract = delivered
Container = rented
```

وتأكد من عدم إنشاء:

```text
duplicate work order
duplicate appointment
duplicate service request
duplicate financial transaction
```

---

# 6. AUTOMATIC INVOICE

هذه أهم نقطة في دورة الفوترة.

عند إنشاء العقد/التخصيص الذي يستحق الفوترة:

**يجب أن تُنشأ الفاتورة تلقائيًا من بيانات العقد، وليس أن يضطر المستخدم لإعادة إدخال البيانات يدويًا.**

الفاتورة يجب أن تعرف تلقائيًا:

```text
Customer
Site
Contract
Container
Container Type
Billing Period
Start Date
End Date
Unit Price
Quantity
VAT
Subtotal
Total
```

ولا تستخدم:

```text
"تفاصيل إضافية"
```

كواجهة لعرض البيانات.

يجب أن تكون البيانات ذات أسماء واضحة ومفهومة.

---

# 7. INVOICE UX

راجع صفحة الفاتورة نفسها.

لا أريد صفحة تعرض:

```text
تفاصيل إضافية
تفاصيل إضافية
تفاصيل إضافية
[object Object]
```

هذا **BUG وليس تصميمًا مقبولًا**.

أعد تنظيم صفحة الفاتورة كواجهة Enterprise واضحة:

### رأس الفاتورة

```text
رقم الفاتورة
الحالة
تاريخ الإصدار
تاريخ الاستحقاق
```

### العميل

```text
اسم العميل
الجوال
الموقع
```

### العقد

```text
رقم العقد
الفترة
الحالة
```

### الحاوية

```text
Container ID
Container Type
```

### بنود الفاتورة

**الحاويات هي الأصناف الأساسية في هذا النظام.**

مثال:

| الحاوية | النوع | الفترة | الكمية | السعر | الإجمالي |
| ------- | ----- | ------ | -----: | ----: | -------: |

ثم:

```text
Subtotal
VAT
Total
Paid
Remaining
```

### الحالة

استخدم الحالات المحاسبية الصحيحة:

```text
Draft
Due
Partially Paid
Paid
Overdue
Cancelled
```

مع ترجمتها العربية المناسبة.

---

# 8. CUSTOMER PROFILE LINKS

يجب أن تكون الفاتورة مرتبطة فعليًا في كل الأماكن المناسبة.

من ملف العميل:

```text
Customer
 ├── Contracts
 ├── Invoices
 ├── Payments
 └── Financial Summary
```

ومن العقد:

```text
Contract
 ├── Customer
 ├── Container
 ├── Work Orders
 ├── Invoice
 └── Payments
```

ومن الفاتورة:

```text
Invoice
 ├── Customer
 ├── Contract
 ├── Container
 └── Payments
```

لا أريد روابط شكلية.

اختبر فتح كل رابط فعليًا.

---

# 9. PARTIAL PAYMENT

على فاتورة قيمتها:

```text
1000
```

نفذ:

```text
Payment #1 = 500
```

يجب أن تصبح:

```text
Invoice Total = 1000
Paid = 500
Remaining = 500
Status = partially_paid
```

ثم نفذ:

```text
Payment #2 = 500
```

ويجب أن تصبح:

```text
Paid = 1000
Remaining = 0
Status = paid
```

---

# 10. PAYMENT IDEMPOTENCY

أعد إرسال Payment #1 بنفس:

```text
Idempotency-Key
```

يجب أن تكون النتيجة:

```text
HTTP 200
idempotent = true
same Payment ID
```

ولا يجوز:

```text
Paid = 1500
```

أو إنشاء Payment جديد.

تحقق من قاعدة البيانات فعليًا.

---

# 11. OVERPAYMENT

حاول دفع:

```text
100
```

بعد أن تصبح الفاتورة:

```text
Paid = 1000
Remaining = 0
```

يجب رفض العملية.

مثلاً:

```text
HTTP 422
```

مع خطأ واضح مثل:

```text
INVOICE_ALREADY_PAID
```

أو الكود الصحيح الموجود فعليًا في النظام.

والأهم:

**لا يحدث أي mutation مالي.**

---

# 12. FINANCIAL INTEGRITY

بعد كل عملية مالية افحص:

```text
Debit
Credit
Journal Entries
Journal Lines
Financial Transactions
```

ويجب دائمًا:

```text
Debit = Credit
```

وتحقق من:

```text
Orphan Transactions = 0
Orphan Journal Lines = 0
Unbalanced Journals = 0
Duplicate Operation Keys = 0
```

---

# 13. PICKUP / RETURN

بعد اكتمال الفوترة والسداد، أنشئ أمر:

```text
Pickup / Return
```

ويجب أن يذهب إلى:

```text
work_orders
```

وليس:

```text
service_requests
```

نفذ دورة السائق:

```text
accepted
started
en_route
arrived
completed
```

ثم تحقق:

```text
Contract = returned
Container = available
Work Order = completed
```

---

# 14. EMPTYING

اختبر عملية:

```text
Emptying
```

وتأكد أنها:

```text
work_order
```

وليست:

```text
service_request
```

**مهم جدًا:**

لا تجعل Emptying يتصرف مثل Delivery.

لا يجوز أن يؤدي التفريغ وحده إلى تغيير:

```text
Contract → delivered
Container → rented
```

إلا إذا كان منطق العمل الحالي ينص صراحةً على ذلك.

يجب احترام نوع العملية.

---

# 15. NO FINANCIAL SIDE EFFECTS

عمليات:

```text
Delivery
Pickup
Emptying
Driver Assignment
Appointment
```

ليست عمليات مالية بحد ذاتها.

لا تنشئ:

```text
Journal Entry
Financial Transaction
Payment
Invoice
```

إلا إذا كان هناك Business Rule صريح لذلك.

---

# 16. TOAST SYSTEM

اختبر الواجهة فعليًا.

كل:

* نجاح
* فشل
* Validation
* حفظ
* سداد
* إنشاء
* حذف
* رفض

يجب أن يظهر Toast:

**أعلى منتصف الشاشة.**

وليس:

```text
window.alert()
```

وليس أسفل الشاشة.

ولا تكرر نفس Toast مرتين.

---

# 17. PAYMENT WINDOW

اختبر نافذة السداد من **كل الأماكن**:

```text
Customer Profile
Contract
Invoice
Payments
```

عند فتحها من فاتورة:

يجب أن يتم تعبئة:

```text
Customer
Invoice
Contract
Amount
Remaining
Allocation
```

تلقائيًا.

ولا يجب أن يظهر:

```text
اختر عقداً واحداً على الأقل
```

إذا كانت الفاتورة والعقد محددين بالفعل.

ولا يجب أن يحتاج المستخدم لإدخال Allocation يدويًا في الحالة الطبيعية.

---

# 18. PHP / NODE PARITY

يجب اختبار نفس السيناريو على:

```text
Node API
PHP Hostinger API
```

والنتائج يجب أن تكون متطابقة في:

* Invoice
* Payment
* Idempotency
* Work Order
* Contract Operation
* Financial validation

إذا ظهر اختلاف:

**أصلحه، ولا تكتب أنه "مقبول".**

---

# 19. LIVE DATABASE VERIFICATION

بعد نهاية الدورة:

نزّل نسخة قاعدة الإنتاج الحالية.

نفذ:

```text
PRAGMA quick_check;
PRAGMA integrity_check;
```

وتحقق من:

```text
Unbalanced Journals = 0
Orphan Transactions = 0
Orphan Journal Lines = 0
Duplicate Operation Keys = 0
```

ثم قارن Dataset الذي أنشأته من البداية للنهاية.

---

# 20. PRODUCTION SAFETY

ممنوع:

* حذف بيانات قديمة
* استبدال `data/sabaik.db`
* رفع قاعدة محلية
* reset production
* migration destructive
* حذف سجلات اختبار قديمة غير تابعة لـDataset الحالي

قبل أي تعديل على Production:

```text
BACKUP
→ VERIFY BACKUP
→ DEPLOY
→ SHA VERIFY
→ LIVE E2E
```

---

# 21. إذا ظهر Bug

لا تتوقف وتكتب:

```text
PARTIAL
```

بدلًا من ذلك:

```text
REPRODUCE
↓
IDENTIFY ROOT CAUSE
↓
PATCH NODE
↓
PATCH PHP
↓
PATCH FRONTEND
↓
BUILD
↓
REGRESSION
↓
DEPLOY
↓
SHA VERIFY
↓
RE-RUN LIVE E2E
```

كرر ذلك حتى تمر الدورة.

**لا تعتبر فشل FTP أو أي عائق مؤقت نهاية للمهمة إذا أصبحت قناة النشر متاحة.**

---

# 22. IMPORTANT — لا تعتمد على القراءة فقط

الاختبار الحالي وصل إلى مرحلة ممتازة من:

```text
Deployment PASS
SHA PASS
Database Integrity PASS
Authentication PASS
Security PASS
```

لكن هذا **ليس شهادة تشغيلية كاملة**.

الآن نحتاج إثباتًا حقيقيًا للـBusiness Flow.

لذلك لا يكفي أن تجد:

```text
Invoices = 5
Payments = 6
Work Orders = 16
```

يجب أن تنشئ Dataset جديدًا وتثبت أن:

```text
Customer
→ Contract
→ Work Order
→ Driver
→ Delivery
→ Invoice
→ Partial Payment
→ Full Payment
→ Idempotency
→ Overpayment Protection
→ Pickup
→ Emptying
→ Final State
```

يعمل بالكامل.

---

# 23. FINAL REPORT

لا ترسل تقريرًا من نوع:

```text
FINAL SCORE: 7/10
PARTIAL
NOT TESTED
```

إلا إذا كان هناك **عائق حقيقي لا يمكن تجاوزه**.

وفي هذه الحالة اذكر:

```text
EXACT BLOCKER
EXACT REQUEST
EXACT EVIDENCE
```

أما إذا كان الكود هو المشكلة:

**أصلحه وأعد الاختبار.**

---

## شهادة النجاح المطلوبة

في النهاية أريد تقريرًا بهذا الشكل:

```text
FINAL PRODUCTION CERTIFICATION

Deployment              PASS
Backup                  PASS
Live SHA                PASS
Database Integrity      PASS

Authentication          PASS
Security                PASS

Customer → Contract     PASS
Contract → Work Order   PASS
Work Order → Driver     PASS
Delivery                PASS
Invoice Creation        PASS
Invoice Idempotency     PASS

Partial Payment         PASS
Full Payment            PASS
Payment Idempotency     PASS
Overpayment Protection  PASS

Customer Invoice Link   PASS
Contract Invoice Link   PASS
Invoice Payment Link    PASS

Pickup                  PASS
Emptying                PASS

Invoice UX              PASS
Payment UX              PASS
Toast System            PASS

Financial Core          PASS
Double Entry            PASS
Node/PHP Parity         PASS
Regression Suite        PASS

LIVE E2E                PASS
PRODUCTION STABILITY    PASS

FINAL SCORE             10/10
```

**ابدأ التنفيذ الآن من آخر نقطة وصلت إليها. لا تعد للخلف ولا تعيد كتابة الخطة. نفّذ، اختبر، أصلح، أعد الاختبار، ثم أغلق الدورة بشهادة نهائية مبنية على أدلة فعلية.**
