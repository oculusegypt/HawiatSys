# 🔴 FINAL HANDOFF — TAQI GROUP / taqigroup.com

## استكمال مباشر من آخر Checkpoint — ممنوع إعادة الـAudit أو استهلاك الـTokens في التحليل المكرر

أنت Agent جديد تستلم مشروع **taqigroup.com** من Agent سابق توقف بسبب quota.

**اقرأ هذه الوثيقة باعتبارها Checkpoint تنفيذي إلزامي، وليس طلبًا لإعادة التدقيق.**

المطلوب هو **الاستكمال من آخر أمر وصل إليه الـAgent السابق ثم الإغلاق النهائي**.

---

# 1. قاعدة أساسية — لا تبدأ من الصفر

ممنوع تمامًا:

* إعادة Full Audit.
* إعادة قراءة المشروع كاملًا.
* إعادة فحص الملفات التي تم فحصها بالفعل بدون سبب.
* إعادة تنفيذ الإصلاحات المنتهية.
* إنشاء نظام SEO جديد بدل النظام الموجود.
* إعادة تصميم لوحة التحكم.
* تغيير Architecture بلا ضرورة.
* تغيير business logic.
* تغيير الأسعار أو الخدمات أو البيانات التجارية لمجرد التنظيف.
* تغيير domain.
* تغيير routes الموجودة.
* إعادة إنشاء نظام prerender من الصفر.
* الدوران في التحليل النظري.

**ابدأ مباشرة من الحالة الحالية للـworking tree.**

أول ما تفعله:

```bash
git status --short
git diff --stat
git diff --check
```

ثم راجع فقط الـdiff الحالي والملفات التي أضافها/عدلها الـAgent السابق.

---

# 2. ما تم إنجازه قبل استلامك

تم سابقًا تنفيذ ومراجعة إصلاحات واسعة في:

* SEO text generation
* `seoText.ts`
* `useDocumentSEO.ts`
* `prerender.mjs`
* `ServiceDetail.tsx`
* `FloatingContactButtons.tsx`
* static pages
* JSON-LD
* Entity IDs
* phone numbers
* SEO media
* sitemap
* Hostinger build
* PHP API
* SQLite
* legacy branding
* production HTML
* runtime DOM
* SEO quality gate

ثم تم تنفيذ إصلاحات إضافية:

### SEO

* توحيد Meta Description generation.
* جعل descriptions ضمن نطاق منطقي يقارب 120–160 حرفًا.
* Homepage description وصلت إلى حوالي 155 حرفًا.
* معالجة اختلاف description بين prerender/runtime.
* إصلاح provider/entity IDs.
* تثبيت production site URL على:

```text
https://taqigroup.com
```

### Branding

تمت إعادة تسمية صور SEO من:

```text
cleanflow-*
```

إلى:

```text
taqi-*
```

مع تحديث المراجع.

تم التحقق أن:

```text
cleanflow public references = 0
broken taqi assets = 0
SEO image assets = 12/12
```

### Phones

تم تنظيف أرقام قديمة من البيانات/المصادر التي ثبت أنها legacy.

الأرقام التي كان مطلوب فحصها خصوصًا:

```text
0557870222
0580595555
```

وظهر أيضًا أثناء الفحص رقم قديم آخر:

```text
0555888767
```

**لكن القاعدة مهمة جدًا:**

لا تستبدل أي رقم لمجرد أنه غير مألوف.

الرقم يبقى إذا كان مصدره موثقًا داخل المشروع.

---

# 3. الحالة التي أعلنها الـAgent السابق

الـAgent السابق أعلن:

```text
TypeScript = PASS
PHP syntax = PASS
SQLite integrity = PASS
git diff --check = PASS
Browser preview = PASS
Browser console = CLEAN
EADDRINUSE = RESOLVED
```

والـProduction build:

```text
Build = PASS
Prerender = PASS
Generated routes = 135
HTML files = 213
Sitemap = 167 URLs
Hostinger ZIP = PASS
```

SEO:

```text
Homepage description = 155 chars
Raw HTML = PASS
Runtime DOM = PASS
Schema = PASS
FAQ = PASS
Canonical = PASS
```

DATA:

```text
SQLite = PASS
Old phone numbers = CLEAN
Legacy public branding = CLEAN
```

SERVICES:

```text
Web = PASS
Node API = PASS
PHP API = PASS
```

وتم اختبار:

```text
/api/healthz
/api/settings
/api/services
/api/containers
```

وصور SEO عبر PHP.

---

# 4. لكن انتبه: ظهرت ملاحظات أثناء الاستكمال ولم تكن مغلقة نهائيًا

هذه أهم نقطة.

أثناء عمل الـAgent السابق ظهرت مشكلة **canonical/sitemap parity**.

تم العثور على فرق بين:

* HTML canonical inventory
* sitemap URLs

وكان هناك تقريبًا:

```text
142 canonical URLs
167 sitemap URLs
```

أي أن هناك URLs في sitemap لا تظهر بنفس الشكل في prerendered HTML.

ثم اتضح أن جزءًا من الفرق مرتبط بمسارات containers/legacy routes وSPA fallback.

**لا تعتبر هذا PASS من كلام الـAgent السابق.**

يجب أن تتحقق من الحالة الحالية فعليًا.

لكن:

**لا تعيد SEO audit كامل.**

اعمل فقط:

1. inspect current sitemap generator.
2. inspect current prerender route generation.
3. احسب canonical URLs فعليًا من آخر build.
4. احسب sitemap URLs فعليًا.
5. حدد الفرق.
6. قرر هل:

   * يجب prerender المسارات الناقصة،
   * أم إزالة URLs غير indexable من sitemap،
   * أم أن هناك fallback route مشروعًا ويجب التعامل معه بشكل صحيح.

لا تستخدم حلًا شكليًا لإجبار الأرقام على التطابق.

---

# 5. آخر حالة وصل إليها الـAgent قبل quota

في آخر مرحلة كان يعمل على إضافة **SEO Metrics إلى قسم SEO في لوحة التحكم**.

تم بالفعل إنشاء/تعديل:

```text
seoMetrics.ts
SEOPanel.tsx
api-server index.ts
api-index.php
build-hostinger.mjs
seo-quality-gate.mjs
vite.config.ts
App.tsx
```

كما تم إصلاح/تعديل:

```text
seoMedia.ts
AreasIndexPage.tsx
Home.tsx
Blog.tsx
PricingPage.tsx
PackageDetail.tsx
PackagesPage.tsx
prerender.mjs
generate-sitemap.mjs
api-index.php
prepare-seo-drafts.mjs
```

**لا تعكس أيًا من هذه التعديلات.**

آخر رسالة فعلية من الـAgent كانت تقريبًا:

> الفحوصات البرمجية الأساسية نجحت، وأبقيت مسار المعاينة المحلي للمنصة كما هو، بينما يستخدم البناء الإنتاجي اسم المسار المحايد الجديد فقط. سأعيد الآن توليد حزمة Hostinger ثم أشغّل بوابة SEO المختصرة وأتحقق من endpoint والواجهة.

ثم توقف بسبب quota.

إذًا أنت تبدأ تحديدًا من:

### BUILD → SEO GATE → API ENDPOINT → DASHBOARD UI → HOSTINGER ZIP → FINAL VERIFICATION

---

# 6. SEO Metrics — المطلوب إغلاقه

المطلوب أن يحتوي قسم SEO الموجود بالفعل في لوحة الإدارة على **مؤشرات حقيقية مبنية على آخر Production Build**.

لا تعمل Dashboard تجميلية أو أرقام hardcoded.

المؤشرات يجب أن تكون derived من actual source/build/data.

يجب أن تغطي على الأقل:

### Core SEO

* SEO Health
* Homepage Meta Description
* Meta Description Coverage
* Canonical Coverage
* H1 Coverage
* Schema Coverage
* FAQ Coverage
* Sitemap Health
* Sitemap URL Count
* Prerendered Route Count
* Generated HTML Count

### Entity / GEO

* Organization
* LocalBusiness
* WebSite
* WebPage
* Service
* ContactPoint
* AreaServed
* Entity ID consistency

### Content / Assets

* SEO Images
* Missing SEO Images
* Broken Assets
* Legacy Branding References
* Old Phone References

### Production

* Production Domain
* HTTPS
* robots.txt
* sitemap.xml
* Hostinger Build
* PHP API
* Node API

---

# 7. حالة كل Metric

لا تعرض أرقامًا فقط.

كل مؤشر يجب أن تكون له حالة واضحة:

```text
PASS
WARNING
FAIL
NOT VERIFIED
```

والواجهة يمكن أن تعرضها بالعربية/الإنجليزية بشكل واضح، مثل:

```text
PASS — ناجح
WARNING — تحذير
FAIL — فشل
NOT VERIFIED — غير متحقق
```

### مهم جدًا

إذا لم يستطع endpoint إثبات قيمة فعلية:

```text
NOT VERIFIED
```

وليس:

```text
PASS
```

ممنوع fake green dashboard.

---

# 8. مصدر المؤشرات

تم إنشاء:

```text
seoMetrics.ts
```

استخدمه بدل إنشاء implementation آخر.

يجب أن يكون هناك **مصدر واحد للحساب** قدر الإمكان.

المصدر يجب أن يعتمد على:

* آخر production artifacts
* HTML
* sitemap
* JSON-LD
* images
* site settings
* database عند الحاجة

وليس على hardcoded values.

---

# 9. Node + PHP parity

لأن الموقع يعمل على Hostinger، لا تجعل لوحة SEO صحيحة فقط على Node development environment.

يجب أن يكون هناك parity مع PHP production.

تم بالفعل البدء في:

```text
api-index.php
```

أكمل فوق الموجود.

يجب أن يكون endpoint المقابل للـSEO Metrics قادرًا على إعطاء مؤشرات حقيقية في production.

لا تنشئ API مختلفًا جذريًا بين Node وPHP.

---

# 10. Authentication / Admin

قسم SEO موجود داخل:

```text
SEOPanel.tsx
```

ومربوط بالـAdmin routes.

لا تكسر authentication.

Endpoint الـmetrics يجب ألا يصبح public بلا سبب.

حافظ على آلية الحماية الموجودة في المشروع.

وفي حالة:

```text
401 / 403 / 500
```

لا تعرض metrics على أنها PASS.

اعرض:

```text
NOT VERIFIED
```

مع سبب واضح.

---

# 11. UI المطلوب

لا تعيد تصميم لوحة الإدارة.

أضف داخل **قسم SEO الحالي** فقط block/grid واضح باسم مثل:

```text
SEO Health & Production Signals
```

أو اسم عربي مناسب.

يعرض Cards/Metrics بسيطة ومتناسقة مع التصميم الحالي.

كل Card:

```text
اسم المؤشر
القيمة
الحالة
مصدر/معلومة مختصرة عند الحاجة
```

مثال:

```text
Meta Description Coverage
100%
PASS
```

```text
Canonical Coverage
94%
WARNING
```

```text
Legacy Branding
0 references
PASS
```

```text
Sitemap
167 URLs
PASS
```

**لكن لا تستخدم هذه الأرقام كمثال داخل الكود.**

القيمة يجب أن تأتي من endpoint.

---

# 12. لا تجعل dashboard سببًا لتغيير SEO behavior

إضافة metrics يجب أن تكون observational أولًا.

لا تغيّر:

* page content
* routes
* pricing
* services
* business logic
* user data

فقط لأن metric معين ظهر WARNING.

إذا اكتشفت مشكلة حقيقية، أصلحها بأصغر patch آمن.

---

# 13. prerender / SPA / 1238%

كانت هناك مشكلة/ملاحظة سابقة مرتبطة بقياس rendering بنسبة كبيرة جدًا مثل:

```text
1238%
```

لا تعتبر الرقم بحد ذاته حقيقة.

المطلوب:

### Compare:

A. Raw HTML before JS

B. Runtime DOM after JS

وتحقق من وجود:

* title
* description
* canonical
* H1
* main text
* services
* areas
* FAQ
* company data
* internal links
* schema

في raw HTML حيث يفترض أن تكون indexable.

**لا تعتمد على hydration لإظهار المحتوى الأساسي SEO.**

ولا تحول التطبيق إلى static HTML كامل إذا كان ذلك سيكسر functionality.

الهدف:

```text
SEO/GEO content = prerender/server first
JavaScript = interaction/functionality
```

---

# 14. FAQ / GEO

تحقق بشكل targeted فقط أن FAQ:

* موجود في raw HTML للمسارات التي يفترض أن تحتويه.
* ليس مجرد رابط إلى FAQ.
* schema FAQ إن كان مستخدمًا يتطابق مع المحتوى الحقيقي.
* لا يوجد fake FAQ content فقط لمحركات البحث.

---

# 15. Legacy Branding

تم سابقًا اكتشاف أسماء:

```text
cleanflow
sabaik
سبائك
الالماسة
```

لكن لا تعمل global replacement أعمى.

المطلوب فقط:

### Production/public contamination

تحقق من:

* HTML
* JS public bundles إذا كانت ذات صلة
* CSS public output إذا كانت ذات صلة
* image URLs
* sitemap
* robots
* API public responses
* ZIP filenames/paths
* public asset paths

إذا ظهر الاسم فقط داخل:

* comments
* internal historical scripts
* migration names
* localStorage legacy keys
* internal development artifacts

فلا تقم بتغييرها بلا حاجة.

---

# 16. Hostinger archive naming

تم تعديل build بحيث يكون production archive path/name محايدًا.

يجب التحقق من:

```bash
unzip -Z1 <final-zip>
```

والتأكد أن أسماء الملفات/المجلدات العامة داخل الـZIP لا تحتوي legacy branding غير مطلوب.

لكن لا تقم بتغيير internal source directory لمجرد الاسم إذا كان ذلك سيكسر المشروع.

---

# 17. Database

SQLite تم إصلاح/التحقق من integrity.

**لا تبدأ database migration جديدة.**

فقط تحقق من الحالة الحالية إذا احتجت.

خصوصًا:

```text
0557870222
0580595555
0555888767
```

ولا تستبدل أرقامًا حقيقية غير مؤكدة.

إذا وجدت legacy value مثبتة في source/seed/database/production:

* أصلح المصدر.
* ثم regenerate production artifact.

لا تصلح production artifact يدويًا وتترك source ملوثًا.

---

# 18. الصور

SEO media يجب أن تكون:

```text
12/12 موجودة
0 broken
0 cleanflow public references
```

بعد إعادة البناء أعد التحقق.

خصوصًا:

```text
images/seo/
```

وتأكد أن كل reference فعليًا موجود.

---

# 19. لا تعتمد على PASS القديم

كل PASS سابق يعتبر:

```text
baseline
```

وليس إثباتًا للحالة الحالية بعد التعديلات الجديدة.

لذلك بعد انتهاء تعديلاتك:

## يجب تنفيذ:

### Code

```text
TypeScript
PHP syntax
git diff --check
```

### Build

```text
production build
prerender
sitemap
SEO quality gate
```

### Runtime

```text
Web
Node API
PHP API
```

### Browser

* homepage
* SEO dashboard
* لا Console errors
* endpoint metrics
* UI metrics

### Production artifacts

* inspect generated HTML
* inspect sitemap
* inspect robots
* inspect assets
* inspect ZIP
* extract ZIP
* inspect extracted files

---

# 20. Acceptance Gate النهائي

لا تقل:

```text
DONE
PASS
PRODUCTION READY
```

إلا إذا نفذت فعليًا:

```text
TypeScript = PASS
PHP syntax = PASS
git diff --check = PASS

Build = PASS
Prerender = PASS
Sitemap = VERIFIED
SEO Quality Gate = PASS

Raw HTML SEO = VERIFIED
Runtime DOM SEO = VERIFIED
Schema = VERIFIED
FAQ = VERIFIED
Canonical = VERIFIED

SQLite integrity = PASS

Web = PASS
Node API = PASS
PHP API = PASS

SEO Metrics endpoint = PASS
SEO Dashboard Metrics = PASS

No Browser Console errors = PASS

Legacy public branding = CLEAN
Old verified legacy phones = CLEAN
Broken assets = 0

Hostinger ZIP = PASS
Extracted ZIP = VERIFIED
```

وأي بند غير متحقق منه يجب أن يكون:

```text
NOT VERIFIED
```

وليس PASS.

---

# 21. أهم شيء: وفر الـTokens

اعمل بهذه الطريقة:

### Step 1

اقرأ:

```text
git status
git diff --stat
git diff --check
```

### Step 2

راجع فقط:

```text
seoMetrics.ts
SEOPanel.tsx
api-server index.ts
api-index.php
build-hostinger.mjs
seo-quality-gate.mjs
vite.config.ts
App.tsx
```

إذا احتجت ملفًا آخر فقط افتحه.

### Step 3

Typecheck/build سريع.

### Step 4

أصلح errors فقط.

### Step 5

شغّل metrics endpoint.

### Step 6

افحص dashboard.

### Step 7

Build production.

### Step 8

SEO gate.

### Step 9

Hostinger ZIP.

### Step 10

Final targeted verification.

**لا تدخل في Audit جديد.**

---

# 22. إذا وجدت مشكلة جديدة

صنفها فورًا:

```text
BLOCKER
REGRESSION
REAL BUG
WARNING
EXPECTED
NOT VERIFIED
```

ثم عالج فقط:

```text
BLOCKER
REGRESSION
REAL BUG
```

ولا تضيع الوقت على warnings غير المؤثرة.

---

# 23. ممنوع التظاهر بإغلاق المهمة

إذا اكتشفت:

```text
sitemap ≠ canonical inventory
```

أو:

```text
raw HTML missing SEO content
```

أو:

```text
metrics endpoint broken
```

أو:

```text
PHP/Node mismatch
```

لا تخفي المشكلة ولا تعدل الرقم ليصبح أخضر.

أصلح السبب.

---

# 24. المطلوب في النهاية

أريد منك تنفيذ العمل، وليس تقريرًا جديدًا طويلًا.

وفي النهاية فقط أرسل ملخصًا مختصرًا بهذا الشكل:

```text
IMPLEMENTED
- ...

VERIFIED
- ...

SEO DASHBOARD
- ...

SEO METRICS
- ...

BUILD
- ...

HOSTINGER
- ...

REMAINING
- ...
```

وإذا كان كل شيء مغلقًا:

```text
FINAL STATUS
PRODUCTION READY
```

أما إذا بقي شيء غير متحقق:

```text
FINAL STATUS
NOT FULLY VERIFIED
```

مع تحديد البند فقط.

---

# 🔴 قاعدة أخيرة

**أنت لا تستلم مشروعًا جديدًا.**

أنت تستلم مشروعًا وصل إلى مرحلة الإغلاق النهائي، وآخر Agent كان بالفعل داخل:

```text
SEO Metrics → PHP parity → Build → Gate → Hostinger verification
```

لذلك:

**لا تعيد ما تم. لا توسّع النطاق. لا تعمل Audit جديد. لا تضيف Features خارج الطلب. لا تستهلك الـTokens في شرح ما تعرفه الوثيقة بالفعل.**

**أكمل من آخر حالة فعلية في الـworking tree، أصلح ما يظهر، ابنِ، اختبر، ثم أغلق المهمة.**
