أيوه. ابعت له الرسالة دي **كما هي**. دي مش رسالة "كمّل شوية"؛ دي أمر إغلاق مرحلة، وممنوع يعتبر أي جزء منجزًا بدون دليل فعلي.

---

# 🔴 FINAL EXECUTION ORDER — NO PARTIAL WORK / NO LOOPING / NO TOKEN WASTE

اسمع جيدًا:

لقد تكررت نفس المشكلة عدة مرات: تبدأ في تحليل الملفات، ثم تدخل في استكشاف طويل، ثم تعدّل جزءًا، ثم تصل إلى حد الاستخدام قبل إكمال الدورة، وفي النهاية تعطيني تقريرًا محليًا بدل تنفيذ المهمة كاملة.

**هذا انتهى.**

المطلوب الآن هو **إغلاق دورة التنفيذ بالكامل**، وليس كتابة خطة جديدة، وليس إعادة اكتشاف المشروع من الصفر، وليس إنتاج تقرير منتصف الطريق.

## 1. ممنوع إعادة استكشاف المشروع بالكامل

أنت لديك بالفعل كل السياق السابق عن النظام وعن الإصلاحات المنفذة.

**لا تعيد:**

* قراءة المشروع كاملًا.
* إعادة تحليل كل الملفات.
* إعادة قراءة ملفات ضخمة لا علاقة مباشرة لها بالمشكلة.
* إعادة بناء خطة المشروع.
* إعادة اختبار أشياء تم إثباتها PASS بالفعل.
* الدوران بين نفس الملفات بدون تنفيذ.

ابدأ من **آخر حالة وصلنا إليها**.

إذا احتجت ملفًا، اقرأ فقط الجزء المرتبط مباشرة بالمشكلة.

**قاعدة استخدام الموارد:**

> أقل عدد ممكن من عمليات القراءة والبحث + أكبر قدر ممكن من التنفيذ والتحقق.

---

# 2. المشكلة الحالية التي يجب إغلاقها

نحن نبني دورة تشغيل كاملة لمنصة الحاويات:

```text
Customer
   ↓
Customer Site
   ↓
Service Request
   ↓
Contract
   ↓
Container / Asset
   ↓
Work Order
   ↓
Driver Assignment
   ↓
Driver Lifecycle
   ↓
Delivery / Pickup / Emptying
   ↓
Contract / Container State Synchronization
```

وبالتوازي:

```text
Contract
   ↓
Invoice
   ↓
Payment
   ↓
Allocation
   ↓
Invoice Status
   ↓
Financial Transaction
   ↓
Journal Entry
```

**يجب أن تعمل الدورتان بدون خلط بين التشغيل والماليات.**

---

# 3. Requests ليست Work Orders

هذا قرار معماري نهائي.

### `service_requests`

تمثل:

> طلب العميل التجاري.

مثل:

* طلب حاوية.
* طلب خدمة.
* طلب جديد من العميل.

### `work_orders`

تمثل:

> أمر التنفيذ الميداني.

مثل:

* Delivery
* Pickup / Return
* Emptying
* أي عملية تشغيلية مرتبطة بعقد/حاوية.

## ممنوع

لا يجوز للمسارات الجديدة أن تنشئ:

```text
service_request
```

لعمليات:

```text
Delivery
Pickup
Return
Emptying
```

بل يجب إنشاء:

```text
work_order
```

مباشرة.

السجلات التاريخية القديمة لا تُحذف.

---

# 4. يجب إصلاح Work Orders فعليًا وليس شكليًا

أثبت الاختبار المحلي أن هناك فجوات حقيقية.

يجب التأكد أن `Work Orders` تستخدم فعلًا:

```text
work_order
```

وليس مجرد عرض سجلات `service_requests` باسم آخر.

راجع وأصلح المسارات اللازمة بحيث يكون هناك Lifecycle حقيقي:

```text
created
→ assigned
→ accepted
→ started
→ en_route
→ arrived
→ completed
```

مع حماية الانتقالات غير الصحيحة.

---

# 5. Driver Workflow

يجب أن تعمل دورة السائق كاملة.

اختبر فعليًا:

```text
accepted
started
en_route
arrived
completed
```

ويجب ألا يتم تجاوز المراحل عشوائيًا.

كما يجب أن يتم حفظ:

```text
driverStatusHistory
```

كـ **history حقيقي**.

### ممنوع

لا يجوز أن يؤدي تحديث الحالة التالية إلى استبدال التاريخ السابق بالكامل.

يجب أن تكون النتيجة مثل:

```text
accepted
started
en_route
arrived
completed
```

وليس:

```text
completed
```

فقط.

---

# 6. Delivery

عند إكمال Delivery:

```text
Work Order = completed
Contract = delivered
Container = rented
```

مع الحفاظ على العلاقات:

```text
customer
contract
container
site
appointment
work_order
```

ولا يتم إنشاء Service Request إضافي.

---

# 7. Pickup / Return

عند إكمال Pickup / Return:

```text
Work Order = completed
Container = available
Contract = returned / closed حسب lifecycle
```

ولا يجوز أن يعامل النظام Pickup كأنه Delivery.

---

# 8. Emptying

هذه نقطة حرجة.

**Emptying ليست Delivery.**

عند تنفيذ:

```text
EMPTYING
```

لا يجوز:

* تحويل العقد إلى delivered.
* تحويل الحاوية إلى rented بسبب التفريغ.
* تشغيل `syncContainerCompletion` الخاصة بالتسليم إذا لم يكن ذلك صحيحًا.

يجب أن يكون لها Lifecycle تشغيلي مستقل.

اختبرها فعليًا.

---

# 9. Idempotency

اختبر إنشاء العقد مرتين بنفس:

```text
Idempotency-Key
```

النتيجة المطلوبة:

```text
same contract
same workOrder
no duplicate work order
no duplicate appointment
```

ثم أعد نفس العملية مرة أخرى.

يجب أن تكون:

```text
idempotent = true
```

أو ما يعادلها في API.

---

# 10. Billing

يجب إغلاق دورة الفوترة أيضًا.

العقد:

```text
Contract
```

ينشئ فاتورة تلقائيًا حسب منطق النظام.

الفاتورة مرتبطة بـ:

```text
Customer
Contract
Container
Billing Period
```

والحاوية هي **البند الفعلي** وليس بندًا نصيًا عامًا.

---

# 11. Payment

اختبر:

```text
Invoice = 1000
```

ثم:

```text
Payment #1 = 500
Payment #2 = 100
```

يجب أن تكون النتيجة:

```text
Invoice Total = 1000
Paid = 600
Remaining = 400
Status = partially_paid
```

ثم لا تسمح بدفع:

```text
401
```

أو أي مبلغ يتجاوز الرصيد.

---

# 12. أهم Bug سابق — ممنوع تكراره

كان لدينا:

```text
Paid = 600
```

لكن الواجهة تعرض:

```text
Paid = 1200
```

بسبب تكرار احتساب التوزيعات/السداد.

**يجب إثبات أن هذا الخطأ غير موجود.**

لا يكفي TypeScript PASS.

لا يكفي Build PASS.

لا يكفي قراءة API.

يجب إنشاء Dataset جديد وإثبات:

```text
Payment 1 = 500
Payment 2 = 100
Total Paid = 600
Remaining = 400
```

من قاعدة البيانات ومن الـAPI ومن الواجهة إن أمكن.

---

# 13. Payment Idempotency

أعد:

```text
Payment #1
```

بنفس:

```text
Idempotency-Key
```

يجب ألا ينشأ Payment جديد.

يجب أن يكون:

```text
same Payment ID
```

ولا توجد Financial Transaction إضافية.

ولا Journal Entry إضافية.

---

# 14. Financial Integrity

بعد دورة الدفع يجب التحقق من:

```text
Financial Transactions
Journal Entries
Journal Lines
```

ويجب:

```text
Total Debit = Total Credit
```

ولا توجد:

```text
orphan transactions
orphan journal lines
duplicate operation keys
```

---

# 15. Node / PHP PARITY

هذا شرط إلزامي.

لدينا:

```text
Node API
PHP Hostinger API
```

أي إصلاح في Node يجب أن يكون موجودًا في PHP.

خصوصًا:

* Work Orders
* Delivery
* Pickup
* Emptying
* Payment
* Allocation
* Invoice
* Idempotency
* Financial posting

**لا تقبل PASS إذا كان أحد المسارين مختلفًا.**

---

# 16. Toast

كل رسائل:

```text
Success
Error
Warning
Validation
```

يجب أن تظهر في:

> **أعلى منتصف الشاشة**

وليس يمين الشاشة.

ولا تستخدم:

```text
alert()
```

ولا رسائل متفرقة بأنظمة مختلفة.

يجب أن يكون هناك نظام Toast موحد.

---

# 17. UI — لا تكتفِ بالـAPI

يجب التأكد أن الواجهات التي تم إصلاحها فعلًا تعمل:

### Customer

يظهر:

```text
Contracts
Invoices
Payments
Outstanding Balance
```

والروابط تفتح السجل الصحيح.

### Contract

يظهر:

```text
Invoices
Work Orders
Container
Operational Timeline
Payments
```

### Invoice

يظهر بشكل احترافي، وليس:

```text
تفاصيل إضافية
تفاصيل إضافية
تفاصيل إضافية
[object Object]
```

**ممنوع وجود أي raw JSON أو `[object Object]` أو أسماء حقول داخلية للمستخدم.**

### Payment

يعرض:

```text
Customer
Contract
Invoice
Payment Amount
Allocated Amount
Remaining
Payment Method
Status
Date
```

بأسماء عربية واضحة.

---

# 18. "تفاصيل إضافية" ممنوعة

هذا عيب UX واضح.

لا أريد رؤية:

```text
تفاصيل إضافية
تفاصيل إضافية
تفاصيل إضافية
```

بدل البيانات.

كل قيمة يجب أن يكون لها Label حقيقي:

```text
رقم السداد
رقم الفاتورة
العقد
المبلغ
طريقة الدفع
التاريخ
الحالة
المتبقي
```

إذا كان هناك JSON داخلي، حوّله إلى بيانات UI مفهومة.

---

# 19. زر "تجهيز العقد"

هذا الزر كان يحول إلى:

```text
view=settlements
```

وهذا خطأ.

يجب أن يفتح:

> **مساحة تشغيل العقد**

ويعرض العمليات:

```text
تجهيز التسليم
إسناد السائق
بدء التنفيذ
الوصول
إتمام التسليم
طلب السحب
التفريغ
إغلاق / إنهاء حسب الحالة
```

لا علاقة له بالتسويات المالية.

---

# 20. FINAL LIVE E2E

بعد الانتهاء من الإصلاحات:

**لا تكتفِ بالاختبارات المحلية.**

نفذ:

```text
Backup
↓
Deploy
↓
SHA Verification
↓
Database Integrity
↓
Authentication
↓
Customer
↓
Contract
↓
Work Order
↓
Driver
↓
Delivery
↓
Pickup
↓
Emptying
↓
Invoice
↓
Payment 500
↓
Payment 100
↓
Idempotency
↓
Overpayment
↓
Financial Integrity
↓
UI Verification
```

---

# 21. بيانات الاختبار

استخدم Dataset منفصلًا تمامًا:

```text
FINAL-LIVE-E2E-2026
```

وكل سجل يتم إنشاؤه في الاختبار يجب أن يكون قابلًا للتعرف عليه بهذه البادئة.

**لا تستخدم سجلات العملاء الحالية لاختبارات تدميرية.**

ولا تحذف بيانات الإنتاج القديمة.

---

# 22. Backup إلزامي

قبل أي تغيير على الإنتاج:

```text
FTP Authentication
→ public_html access
→ Backup
→ Verify backup
→ SHA
→ Deploy
```

إذا فشل Backup:

**توقف عن النشر.**

لا تقل:

> "الكود جاهز"

وتعتبر المهمة منتهية.

---

# 23. لا تعبث بقاعدة الإنتاج

ممنوع:

* استبدال `data/sabaik.db`
* رفع قاعدة محلية.
* حذف البيانات القديمة.
* تنظيف الإنتاج يدويًا.
* إعادة seed لقاعدة الإنتاج.

إذا احتجت Dataset للاختبار، أنشئه من خلال النظام نفسه.

---

# 24. لا تعتبر Build شهادة

هذه ليست شهادة:

```text
TypeScript PASS
Build PASS
PHP lint PASS
```

هذه فقط:

> Static Validation.

الشهادة الحقيقية هي:

```text
Functional E2E
+
Database Verification
+
Financial Verification
+
UI Verification
+
Production Verification
```

---

# 25. ممنوع التقرير الجزئي

لا تتوقف وتكتب:

> "تم تنفيذ الجزء الأساسي"

أو:

> "المتبقي خطوة النشر"

أو:

> "أحتاج إعادة قراءة الملفات"

أو:

> "سأكمل في الجولة القادمة"

**إلا إذا كان هناك Blocker حقيقي لا تستطيع تجاوزه.**

إذا ظهر Bug أثناء الاختبار:

```text
Detect
→ Reproduce
→ Fix
→ Retest
→ Continue
```

لا تتوقف عند اكتشافه.

---

# 26. ممنوع الدوران في نفس المكان

إذا فشل Script بسبب:

```text
wrong endpoint
wrong import
wrong local server
wrong credentials
```

صححه بسرعة وانتقل.

لا تعيد نفس البحث عشرات المرات.

إذا كنت تعرف الملف والمسار، افتح الجزء المطلوب فقط.

---

# 27. منع استنزاف الـToken / Context

هذه نقطة مهمة جدًا.

**لا تقرأ ملفات ضخمة بالكامل لمجرد الفهم.**

استخدم:

* Search محدد.
* Read ranges صغيرة.
* Search عن function/route محددة.
* لا تعيد فتح نفس الملف إذا لم يتغير.
* لا تعيد قراءة الوثائق السابقة بالكامل.
* لا تعيد تحليل ما تم إثباته.

### ممنوع هذه الحلقة:

```text
Search
→ Read huge file
→ Think
→ Search same thing
→ Read same file
→ Think
→ Stop
```

أريد:

```text
Identify
→ Patch
→ Test
→ Fix
→ Test
→ Deploy
→ Verify
```

---

# 28. لا تنشئ ملفات ضخمة بلا داعٍ

إذا أنشأت:

```text
E2E scripts
reports
logs
archives
snapshots
```

اجعلها مؤقتة ومحدودة.

لا تجعل Logs ضخمة تدخل في السياق.

الاختبار النهائي يكفي أن يخرج:

```text
PASS / FAIL
IDs
Counts
Balances
Hashes
Assertions
```

---

# 29. لا تعيد اختبار الأشياء الثابتة

إذا كان لدينا بالفعل:

```text
Financial Core PASS
Double Entry PASS
PHP lint PASS
Build PASS
Archive PASS
```

لا تعيد تشغيلها عشر مرات بدون سبب.

أعدها فقط بعد تعديل يؤثر عليها.

---

# 30. معيار الإنهاء

لا تعلن:

```text
10/10
```

إلا إذا كانت كل هذه:

```text
Deployment             PASS
Backup                  PASS
Live SHA                PASS
Database Integrity      PASS
Authentication          PASS
Security                PASS

Customer → Contract     PASS
Contract → Work Order   PASS
Work Order → Driver     PASS
Delivery                PASS
Pickup                  PASS
Emptying                PASS

Contract → Invoice      PASS
Invoice Idempotency     PASS
Invoice → Payment       PASS
Partial Payment         PASS
Full Payment            PASS
Overpayment Protection  PASS
Payment Idempotency     PASS

Customer Invoice Link   PASS
Contract Invoice Link   PASS
Invoice Payment Link    PASS

Invoice UX              PASS
Payment UX              PASS
Toast System            PASS

Financial Core          PASS
Double Entry            PASS
Node/PHP Parity         PASS
Regression Suite        PASS
Live E2E                PASS
Production Stability    PASS
```

أي:

```text
NOT TESTED
PARTIAL
CONDITIONAL
BLOCKED
```

تعني أن الشهادة **ليست 10/10**.

---

# 31. والأهم: لا تخلع

أنت الآن عندك كل ما تحتاجه.

**لا تبدأ مشروعًا جديدًا.**

أكمل الإصلاح الحالي حتى النهاية.

إذا اكتشفت أن Work Orders تحتاج Route جديدًا:

> أنشئه.

إذا اكتشفت أن Frontend ما زال يستخدم `serviceRequests`:

> أصلحه.

إذا اكتشفت أن PHP مختلف:

> أصلحه.

إذا اكتشفت أن Payment Allocation يتكرر:

> أصلحه.

إذا اكتشفت أن Emptying يغير حالة Delivery:

> أصلحه.

إذا اكتشفت أن Invoice UI يعرض raw fields:

> أصلحه.

إذا اكتشفت مشكلة في Toast:

> أصلحها.

**لا تسجل المشكلة وتتركني أنا أطلب منك إصلاحها.**

---

# FINAL COMMAND

نفّذ المهمة بهذا الترتيب:

```text
1. Inspect only relevant code
2. Identify remaining gaps
3. Fix all gaps
4. Run local E2E
5. Run regression tests affected by changes
6. Build Node
7. Build Frontend
8. PHP lint
9. Build Hostinger package
10. Verify package
11. Backup production
12. Deploy
13. Verify LIVE SHA
14. Verify production DB integrity
15. Run FINAL-LIVE-E2E-2026
16. Verify UI
17. Verify financial invariants
18. Verify Node/PHP parity
19. Retest every failed assertion
20. Produce FINAL PRODUCTION CERTIFICATION
```

## والقاعدة الأخيرة:

**لا أريد تقريرًا يشرح لي لماذا لم تكمل.**

أريد:

> **تنفيذ → اختبار → إصلاح → إعادة اختبار → نشر → تحقق → شهادة نهائية.**

إذا كان هناك Blocker حقيقي، اذكره فقط مع **الدليل التقني الدقيق** وما الذي تم إنجازه قبله.

غير ذلك:

**كمّل حتى النهاية. لا تتوقف عند أول Bug، ولا تستهلك الـContext في إعادة الاستكشاف، ولا تكرر نفس الطلبات بلا فائدة.**
