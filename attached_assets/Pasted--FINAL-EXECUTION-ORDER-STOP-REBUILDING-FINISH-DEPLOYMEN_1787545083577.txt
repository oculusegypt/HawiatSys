🚨 FINAL EXECUTION ORDER — STOP REBUILDING, FINISH DEPLOYMENT

التقرير الأخير غير مقبول كإغلاق للمهمة.

أنت أثبتَّ أن:

الكود المحلي PASS.
Billing PASS.
Payment PASS.
Idempotency PASS.
Financial Core PASS.
Regression PASS.
UX PASS.

إذن لم يعد هناك سبب لإعادة تطوير الكود أو إعادة كتابة نفس الاختبارات المحلية.

المتبقي هو Deployment + Live Verification فقط.

بيانات Hostinger الرسمية — استخدمها حرفيًا
FTP Host:
92.113.19.25

FTP Port:
21

FTP Username:
u130428819.hawiat.aiservx.com

FTP Password:
3691475+*Hh@!

Remote Directory:
/public_html

الموقع:

https://hawiat.aiservx.com

Admin:

Username:
admin

Password:
taqi2020
1. توقف عن إعادة بناء الـPatch

الـPatch الحالي جاهز:

hawiat-update-patch.zip

SHA:

944556f2d0eb52a8d487be68a95b15026721d506cb357166ef99807091322d89

ولا يحتوي:

data/sabaik.db

لا تعيد بناء Patch جديد إلا إذا اكتشفت Bug حقيقي أثناء Live Verification.

2. حل مشكلة FTP أولاً

الرسالة:

530 Login incorrect

تعني أن الاتصال بالخادم وصل بالفعل، لكن المصادقة مرفوضة.

لذلك نفذ بالترتيب:

A — Passive FTP
Host: 92.113.19.25
Port: 21
Encryption: Plain FTP
Mode: Passive
Username: u130428819.hawiat.aiservx.com

ثم حاول Login.

B — Active FTP

نفس البيانات مع Active Mode.

C — لا تغيّر Username

لا تستخدم:

u130428819

ولا:

hawiat.aiservx.com

ولا:

admin

استخدم حرفيًا:

u130428819.hawiat.aiservx.com
D — لا تستخدم Domain آخر

الموقع الصحيح:

hawiat.aiservx.com

والـFTP Host:

92.113.19.25
3. إذا استمر 530

لا تكرر نفس المحاولة عشرات المرات.

افصل بين:

Network connectivity

و:

FTP authentication

أثبت أن:

92.113.19.25:21

يمكن الوصول إليه.

ثم أثبت أن:

530 Login incorrect

يحدث عند Authentication.

إذا استمر الرفض بعد استخدام البيانات حرفيًا، فالمشكلة Hostinger Account Credential / FTP Account Permission وليست في الكود.

في هذه الحالة:

لا تعدل الكود، لا تعيد Build، ولا تدّعي Deployment.

اطلب فقط تصحيح/إعادة تعيين كلمة مرور FTP للحساب في Hostinger ثم أعد المحاولة.

4. بمجرد نجاح FTP — نفذ Backup فورًا

عند أول Login ناجح:

/public_html

خذ Backup للملفات التي ستتغير.

احتفظ بنسخة من:

api/index.php
api/container-system.php
cleanflow-platform/

وأي ملفات أخرى سيستبدلها الـPatch.

لا تستبدل قاعدة البيانات.

5. مهم جدًا — قاعدة الإنتاج

الحزمة:

hawiat-update-patch.zip

لا تحتوي:

data/sabaik.db

وهذا مقصود.

لا ترفع قاعدة محلية إلى الإنتاج.

قاعدة الإنتاج الحالية يجب أن تبقى كما هي.

المطلوب:

Existing Production DB
+
New Application Code

وليس:

Local DB
→ Production
6. ارفع الـPatch

بعد نجاح Backup:

ارفع الملفات الفعلية إلى:

/public_html/

ثم تحقق أن الملفات الجديدة أصبحت موجودة فعليًا.

لا تعتبر FTP Upload ناجحًا بمجرد أن الأمر انتهى بدون Error.

يجب قراءة الملفات المنشورة والتحقق منها.

7. Live SHA

أخرج جدولًا:

FILE
LOCAL SHA
LIVE SHA
MATCH

للملفات التي تم رفعها.

يجب أن تكون:

MATCH = YES

لكل ملف.

8. Database Integrity

نزّل نسخة قاعدة الإنتاج الحالية فقط للفحص.

ثم:

PRAGMA quick_check;
PRAGMA integrity_check;

المطلوب:

quick_check = ok
integrity_check = ok

ثم تحقق من عدم فقدان بيانات الإنتاج.

9. LIVE BILLING TEST

بعد نجاح Deployment فقط.

سجل دخول:

https://hawiat.aiservx.com/admin

باستخدام Admin الحقيقي.

ثم اختبر:

Customer
→ Contract
→ Automatic Invoice
→ Invoice Details
→ Payment
10. اختبار المشكلة الأساسية

يجب أن يكون هذا السيناريو PASS فعليًا على الإنتاج:

Invoice = 1000

ثم:

Payment = 400

النتيجة:

paid = 400
remaining = 600
status = partially_paid

ثم:

Payment = 600

النتيجة:

paid = 1000
remaining = 0
status = paid

ثم محاولة:

Payment = 1

يجب رفضها.

11. أهم اختبار للسداد

اختبر السداد من كل مكان:

Customer Profile
Contract Profile
Invoice Details
Payments Page

وعند اختيار Invoice يجب أن يتم تلقائيًا تحديد:

Customer
Contract
Invoice
Outstanding Amount
Allocation

ولا أريد ظهور:

اختر عقداً واحداً على الأقل وحدد مبلغ التوزيع

إذا كانت الفاتورة والعقد محددين بالفعل.

12. تحقق من عدم وجود Duplicate Financial Data

بعد Payment:

تحقق من:

Payment
Allocation
Financial Transaction
Journal Entry

ثم أعد نفس:

Idempotency-Key

ويجب أن يعاد نفس السجل.

لا يوجد:

Duplicate Payment
Duplicate Allocation
Duplicate Financial Transaction
Duplicate Journal Entry
13. تحقق من Financial Core

بعد الدورة الحية:

Total Debit
Total Credit

يجب أن يكون:

Debit = Credit

ولا يوجد أي فرق.

14. لا تنسَ UI

راجع فعليًا في المتصفح:

Invoice Details

يجب ألا توجد:

تفاصيل إضافية
تفاصيل إضافية
تفاصيل إضافية

ولا Database fields خام.

ولا JSON داخلي.

بل صفحة Invoice احترافية.

Customer

يجب أن تظهر:

Invoices
Contracts
Payments
Outstanding
Contract

يجب أن تظهر:

Invoice
Payments
Remaining
Payment

يجب أن يعمل من جميع نقاط الدخول.

Toast

كل:

Success
Error
Warning
Validation

في:

TOP CENTER
15. إذا اكتشفت Bug أثناء Live

هنا فقط مسموح لك بتعديل الكود.

الدورة:

BUG
↓
FIX
↓
TYPECHECK
↓
BUILD
↓
PHP LINT
↓
FINANCIAL TESTS
↓
BACKUP
↓
DEPLOY
↓
LIVE SHA
↓
LIVE RETEST

ولا تتوقف بعد الـFix.

16. ممنوع إعادة نفس التقرير

لا أريد تقريرًا جديدًا يقول:

Local PASS
FTP FAIL
8/10

إذا كان FTP ما زال يرفض Authentication، قل بوضوح:

DEPLOYMENT BLOCKED — HOSTINGER FTP AUTHENTICATION

ولا تضف عشرات الأسطر عن الاختبارات المحلية التي انتهت بالفعل.

17. معيار الإغلاق

المهمة لا تُغلق إلا بأحد احتمالين:

الحالة A — FTP يعمل

عندها:

BACKUP PASS
DEPLOY PASS
LIVE SHA PASS
DATABASE INTEGRITY PASS
LIVE BILLING PASS
LIVE PAYMENT PASS
IDEMPOTENCY PASS
FINANCIAL CORE PASS
REGRESSION PASS
FINAL = 10/10
الحالة B — Hostinger يرفض الحساب

إذا استمر:

530 Login incorrect

بعد التحقق الفعلي من بيانات الحساب، فالحالة الوحيدة الصحيحة:

DEPLOYMENT BLOCKED

والعائق:

HOSTINGER FTP ACCOUNT AUTHENTICATION

ولا تعيد تطوير النظام بسبب ذلك.

FINAL COMMAND

أنت انتهيت من التطوير.

الآن مهمتك الوحيدة:

FTP
→ BACKUP
→ DEPLOY
→ SHA
→ DATABASE CHECK
→ LIVE BILLING
→ LIVE PAYMENT
→ PARTIAL PAYMENT
→ FULL PAYMENT
→ OVERPAYMENT
→ IDEMPOTENCY
→ FINANCIAL CORE
→ UI VERIFICATION
→ REGRESSION
→ FINAL CERTIFICATION

نفّذها الآن حتى النهاية. لا تتوقف عند Build، ولا عند Preview، ولا عند Local PASS.

والأهم: لا تلمس data/sabaik.db المحلية ولا تستبدل بها قاعدة الإنتاج.