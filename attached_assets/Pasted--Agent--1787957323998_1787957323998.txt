نفّذ الآن ولا تكتفِ بالتحليل أو إعداد خطة جديدة.

أنت تستلم المشروع من Agent سابق وصل إلى مرحلة متقدمة، والتغييرات الحالية موجودة بالفعل في الـworking tree. **ممنوع إعادة تدقيق المشروع من الصفر، وممنوع إعادة تنفيذ ما تم إنجازه، وممنوع التوقف عند تقرير أو اقتراحات. المطلوب إغلاق كل المشاكل المتبقية فعليًا ثم Build + Verification + Production Package.**

## الحالة التي تستلم منها

تم بالفعل تنفيذ وإثبات:

* SEO prerender.
* Sitemap generation.
* Canonical normalization.
* Canonical ↔ Sitemap parity.
* SEO metrics داخل لوحة التحكم.
* Node/PHP parity للمؤشرات.
* Meta descriptions.
* JSON-LD infrastructure.
* Internal linking infrastructure.
* SEO media handling.
* Hostinger build.
* PHP syntax.
* TypeScript.
* `git diff --check`.
* SQLite integrity.
* Runtime smoke tests.
* حماية `/api/admin/seo/metrics`.
* Production URL = `https://taqigroup.com`.
* Sitemap = 165 URL.
* Canonical ↔ Sitemap = 165/165.
* Build وPrerender وArchive سبق أن نجحوا.

لكن **آخر ناتج فعلي للوحة SEO كشف أن الإغلاق السابق كان مبكرًا**، والقيم الحالية هي:

```text
Canonical Coverage       WARNING  100%   286/287
Canonical ↔ Sitemap      PASS    100%   165/165
Sitemap Health            PASS           165/165
Structured Data          WARNING        286/287
FAQ / GEO Content        FAIL           109/287
Internal Linking         WARNING 100%   286/287
SEO Media                WARNING        9/12
Legacy Branding          FAIL           4 files
```

هذه ليست أرقامًا تجميلية. **يجب تحديد سبب كل حالة وإغلاقها من المصدر.**

---

# المطلوب الآن — بدون توقف

## 1. Canonical Coverage — 286/287

حدد الملف/الصفحة الوحيدة التي لا تحتوي canonical في **آخر production output الفعلي**.

لا تفترض أنها المشكلة نفسها التي كانت موجودة سابقًا.

اعمل:

1. تحديد الملف بدقة.
2. تحديد لماذا لا يتم توليد canonical.
3. إصلاح المصدر أو prerender.
4. إعادة build.
5. إثبات:
   `287/287`.

**ممنوع تعديل المؤشر ليعتبر الصفحة صحيحة بدون إصلاح HTML نفسه.**

---

## 2. Structured Data — 286/287

حدد الصفحة الوحيدة التي لا تحتوي structured data صالحًا.

افحص:

* JSON-LD في raw HTML.
* نوع الـschema.
* صحة JSON.
* ارتباطه بمحتوى الصفحة.
* هل الصفحة فعلًا تحتاج Schema أم أنها صفحة مستثناة منطقيًا؟

إذا كانت صفحة عامة قابلة للفهرسة، أصلحها من المصدر.

إذا كانت الصفحة يجب أن تكون مستثناة، فليكن الاستثناء **صريحًا ومنطقيًا في metric نفسه**، وليس إخفاءً للخطأ.

الهدف:

```text
287/287
```

أو استثناء موثق فقط إذا كانت الصفحة فعلًا غير قابلة للفهرسة.

---

## 3. FAQ / GEO — 109/287 FAIL

**هنا ممنوع تمامًا إضافة FAQ مكرر أو حشو SEO.**

المطلوب أولًا تحديد ما الذي يقيسه المؤشر.

قسّم الصفحات إلى:

* صفحات SEO تجارية/خدمية تستحق FAQ/GEO.
* صفحات قانونية.
* صفحات تفاعلية.
* صفحات نظامية.
* صفحات Listing/Collection.
* صفحات لا تحتاج FAQ منطقيًا.

لا تجعل الهدف:

```text
287/287
```

بشكل أعمى.

المطلوب هو **تصحيح منطق المؤشر ليقيس الصفحات التي يجب أن تحتوي FAQ/GEO فعلًا، مع إضافة المحتوى فقط للصفحات التي يخدم المستخدم وSEO فيها**.

لا تضف نفس FAQ لكل الصفحات.

يجب أن تكون الأسئلة والأجوبة:

* مرتبطة بالصفحة.
* مفيدة فعليًا.
* غير مكررة.
* متوافقة مع محتوى الصفحة.
* موجودة في HTML/JSON-LD عند الحاجة.

ثم أعد حساب المؤشر.

---

## 4. Internal Linking — 286/287

حدد الصفحة الوحيدة التي تفشل.

لا تعدّل metric لإجبارها على PASS.

حدد سبب غياب الرابط الداخلي القابل للفحص.

إذا كانت صفحة عامة قابلة للفهرسة، أضف لها رابطًا داخليًا طبيعيًا من مصدر الصفحة.

إذا كانت صفحة لا تحتاج ذلك، اجعل منطق metric يعرف الاستثناء بشكل صريح.

الهدف النهائي:

```text
287/287
```

أو استثناء مبرر برمجيًا إذا كان منطقيًا.

---

## 5. SEO Media — 9/12

لا تحاول رفع الرقم إلى 12 لمجرد أن الرقم يبدو أفضل.

افحص الـ12 ملفًا/صورة التي يعتبرها النظام SEO media:

* ما الذي يستخدمه فعلًا؟
* ما الذي لا يستخدمه؟
* هل الثلاثة غير المستخدمة غير ضرورية؟
* هل هي بقايا legacy؟
* هل صفحات معينة يفترض أن تستخدمها؟
* هل هناك fallback يجعلها غير مستخدمة؟

ثم اختر الحل الصحيح:

### إذا كانت الصور الثلاثة غير ضرورية:

نظّف manifest/config واجعل metric يعكس الـSEO media الفعلية المطلوبة.

### إذا كانت مطلوبة:

اربطها بالصفحات المناسبة فعليًا وأثبت وجودها في الناتج.

**ممنوع إضافة references وهمية فقط لرفع النسبة.**

---

## 6. Legacy Branding — FAIL / 4 files

هذه النقطة تحتاج دقة شديدة.

**ممنوع Global Replace.**

افحص الملفات الأربعة واحدًا واحدًا، وحدد:

* اسم الملف.
* مكان ظهور `cleanflow`.
* هل هو public production artifact؟
* هل هو branding قديم فعلًا؟
* هل هو internal identifier؟
* هل CleanFlow هنا اسم منتج حالي/منصة وليس legacy branding؟

لا تعتبر `CleanFlow Platform` تلقائيًا legacy.

خصوصًا أن المشروع يحتوي على:

```text
Taqi Group
CleanFlow Platform
```

فلا تحذف اسم CleanFlow إذا كان اسم المنتج الحالي.

المطلوب إزالة **التسريب القديم فقط**، وليس كسر هوية المنتج الحالي.

إذا كانت المشكلة في:

* `BUILD_INFO.json`
* `UPLOAD_INSTRUCTIONS.txt`
* service worker/cache identifiers
* generated artifact names
* public metadata

فأصلح المصدر الذي يولدها.

لا تصلح الناتج يدويًا فقط إذا كان سيعود في Build القادم.

بعد الإصلاح:

```text
Legacy Branding = PASS / 0 unintended legacy references
```

مع الحفاظ على أي `CleanFlow` reference مقصود ومشروع.

---

# مهم جدًا: لا تعبث بالمؤشرات

المؤشرات يجب أن تكون **مبنية على production artifact الحقيقي**.

لا تستخدم:

* hardcoded numbers.
* fake PASS.
* استثناءات عامة.
* تجاهل الملفات الفاشلة.
* تقليل denominator بدون سبب.
* قراءة source بدل الناتج النهائي.

المصدر الصحيح:

```text
Build
→ Prerender
→ Production output
→ Metrics
→ Quality Gate
```

Node وPHP يجب أن يعطيا نفس النتيجة.

---

# لا تعيد المشاكل التي تم حلها

لا ترجع:

* sitemap parity failure.
* missing container prerender.
* package/container fallback bug.
* broken SEO images.
* old phone numbers.
* old SEO image filenames.
* incorrect production URL.
* duplicate sitemap URLs.
* malformed URLs.
* noindex URLs داخل sitemap.
* Preview URLs داخل sitemap.

حافظ على:

```text
165 sitemap URLs
165/165 canonical parity
https://taqigroup.com
```

إلا إذا أثبتت الفحوصات أن تغييرًا ضروريًا ومبررًا.

---

# التنفيذ الإجباري

نفّذ بالترتيب:

1. Inspect current git diff.
2. Identify the exact failing files/pages from the latest production artifact.
3. Fix the source.
4. Fix prerender/generator logic if required.
5. Fix metrics logic only where its definition is actually wrong.
6. Run TypeScript.
7. Run PHP syntax check.
8. Run `git diff --check`.
9. Build Hostinger.
10. Run prerender.
11. Run SEO quality gate.
12. Recalculate all SEO metrics from the newly generated artifact.
13. Compare Node vs PHP metrics.
14. Extract the final ZIP and inspect it.
15. Search the final public artifact for unintended legacy branding.
16. Verify sitemap ↔ canonical parity.
17. Verify all referenced assets.
18. Start/restart required workflows only if needed.
19. Test the SEO dashboard itself.
20. Capture final screenshot of the SEO section if the environment supports it.
21. Only after ALL checks pass, declare completion.

---

# Definition of Done

لا تقل "تم" إلا إذا وصلت إلى نتيجة فعلية مثل:

```text
Canonical Coverage       PASS
Structured Data          PASS
FAQ / GEO                PASS أو coverage منطقي موثق
Internal Linking         PASS
SEO Media                PASS
Legacy Branding          PASS
Canonical ↔ Sitemap      PASS
Sitemap Health           PASS
Build                    PASS
Prerender                PASS
TypeScript               PASS
PHP                      PASS
Archive                  PASS
Node/PHP parity          PASS
Runtime                  PASS
```

وإذا بقيت أي WARNING أو FAIL:

**لا تخفيها ولا تغيّر اسمها إلى PASS. أصلح السبب الحقيقي أولًا.**

---

# والأهم

لا تكتب لي خطة جديدة.

لا تقل:

* "سأقوم لاحقًا..."
* "يمكن تحسين..."
* "الخطوة التالية..."
* "يوصى بـ..."
* "سأراجع..."

**نفّذ الآن.**

إذا واجهتك مشكلة أثناء التنفيذ، حلها من المصدر ثم أكمل.
إذا احتجت تعديل أكثر من ملف، عدّلها.
إذا احتجت إعادة Build أكثر من مرة، أعده.
إذا فشل الاختبار، لا تتوقف عند التقرير؛ أصلح السبب وأعد الاختبار.

**الهدف هو إغلاق المهمة بالكامل، وليس إنتاج تقرير عن سبب عدم إغلاقها.**
